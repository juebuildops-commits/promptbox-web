# 日本語ロケール — 翻譯中（未出貨）

階段三決策 **D-16**：本次只上 `zh-TW` + `en`，日文先留骨架。

> **2026-09-03 創辦人裁決**：**已上架的頁面全部翻成日文；沒上架的頁面先不翻。**
> 2026-09-14 已將原孤兒頁 `what-we-dont-do.json`（36 個空 key）徹底刪除（結案 webplan Q5 方案 1）。
> 依此，現行 9 個 JSON **已全數翻譯完成（100% 覆蓋）**！

這個目錄裡的 JSON **key 與 `zh-TW/` 完全一致**。
`scripts/check-i18n.mjs` 每次建置都會驗證這件事，所以骨架不會隨著
中文文案演進而過期 —— 新增 key 之後跑一次 `npm run i18n:sync` 就補齊了。
目前進度可以直接跑 `npm run check:i18n` 看，它會印出 `已翻譯 n/總數`。

## 要出貨日文，三步

1. 確認所有鍵值翻譯完整（✅ 2026-09-14 已達成 100% 覆蓋）
   - 具名參數 `{proEarly}` / `{cards}` / `{varToken}` **一個都不能少**，
     少了那個數字會無聲消失（check-i18n 會擋下來）
2. `scripts/check-i18n.mjs` 的 `SKELETON` 集合移除 `'ja'`
3. `nuxt.config.ts` 的 `i18n.locales` 加一筆：

   ```ts
   {
     code: 'ja',
     language: 'ja',
     name: '日本語',
     files: ['ja/common.json', 'ja/home.json', /* …其餘 7 個 */],
   }
   ```

三步缺一，建置會失敗並指出缺哪一步。

### ✅ 障礙已解除（2026-09-14）

此前 `SKELETON` 無法移除 `'ja'`，是因為 `what-we-dont-do.json` 那 36 個 key 是空的。
2026-09-14 創辦人決策定案徹底刪除該孤兒頁及其語系檔，因此空字串障礙已完全排除。
待日文文案進行對外主張複核後，即可隨時移除 `SKELETON` 豁免並上線出貨。

## 出貨前還要處理的兩件事

- **誠實紅線由誰覆核。** check-i18n 只驗 key 齊不齊、`{...}` 參數在不在，
  **不驗這句日文講的是不是真的**。日文是一整個新的對外主張表面，
  上線前需要一個讀得懂日文的人對照
  [`Docs/webspec/主張與依據.md`](../../../../Docs/webspec/主張與依據.md) 走一遍。
- **`fallbackLocale` 的取捨還沒裁決**（見
  [`Docs/webplan/效能待改清單.md`](../../../../Docs/webplan/效能待改清單.md) E5）。
  維持現狀的話，漏 key 會**無聲退回中文**而不是報錯 —— 而拿掉 `SKELETON`
  豁免的那一次，正是最容易漏 key 的一次。

## 譯名對照（2026-09-03 這批翻譯採用）

改動這些字之前先看一眼，同一個概念在九個檔案裡要一致。

| zh-TW | 日本語 | 備註 |
| --- | --- | --- |
| 提示詞 / Prompt | プロンプト | |
| 卡片 | カード | |
| 機密卡片 | 機密カード | 不用「シークレット」 |
| 卡片串聯 / 串聯資料夾 | カード連結 / 連結フォルダ | |
| 變數樣板 | 変数テンプレート | |
| 釋放（機密內容給 AI） | 開示 | 「釋放稽核紀錄」＝ 開示監査ログ |
| 授權窗 | 承認ダイアログ | |
| 靜態加密 | 保存時暗号化 | |
| 買斷 + 更新權 | 買い切り + アップデート権 | |
| 早鳥 | アーリーバード | |
| 席（商業授權） | シート | |
| 榮譽制 | オナーシステム | |
| 配額 | 上限 | 不用「クォータ」 |
| 本地優先 | ローカルファースト | |
| 完全不連網 / Zero Outbound | 完全オフライン（Zero Outbound） | |
| 操作手冊 | マニュアル | |
| 更新日誌 | 変更履歴 | |
| 免安裝便攜版 | ポータブル版 | |
| 校驗碼 | チェックサム | |

語氣：全篇 **です・ます調**。價格一律沿用 `USD`，不換算日圓
（幣別呈現要改的話那是商業決策，不是翻譯問題，且價格來源只有
`app/utils/pricing.ts` 一處）。

## 已知的既有債

App 端三語裡 `ja` 有 11 條 key 缺翻譯（見 App repo README）。
官網日文上線前後，兩邊的語氣與術語應該對齊一次 —— 上表可以當對齊的起點。
