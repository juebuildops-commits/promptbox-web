---
version: "v3.9.2"
date: "2026-09-10"
summary: "Chain Secret Boundary（串聯的機密邊界）——機密卡片的變數值在串聯裡直接代入，不再要你把金鑰在不受保護的欄位重打一次。"
---

- <span class="text-lg">🔑</span><span>**解鎖之後，機密卡片自己的變數值會直接代入串聯卡片**：先前串聯展開機密卡片的內容時，那張卡加密存好的變數值繼承不到，面板上那一格是空的——等於我們自己開了一個不加密的欄位，請你把金鑰再打一次。現在那一格自己就不見了。代入的值只活在你剛用密碼換來的那份預覽裡，**不新增任何明文儲存**：關窗、換卡、鎖定螢幕、閒置 10 分鐘一律清除。</span>
- <span class="text-lg">🚫</span><span>**機密卡片宣告、但沒有填值的變數，不再出現在串聯卡片的面板上**：那些欄位屬於機密卡片，該在機密卡片裡填。</span>
- <span class="text-lg">👁️</span><span>**已經填過值的那一格改為唯讀顯示，不會直接消失**：那些值本來就以明文存在資料庫裡，把欄位藏起來只會讓你看不到它、東西還在。因此保留唯讀的那一列與琥珀警語，由你自己決定要不要清掉——我們不會靜默刪除你的資料。</span>
- <span class="text-lg">🟠</span><span>**串聯的展開結果在解鎖後仍維持橘色標示**：先前解鎖之後會變回中性色，而那正是裡面真的有機密內容的時候。一併提醒：解鎖後按複製鈕，複製到的是代入後的真金鑰，不是佔位符。</span>
- <span class="text-lg">💬</span><span>**串聯卡片的金鑰警語不再指向一顆不存在的按鈕**：串聯卡片不能設密碼，因此那則提醒保留、但不再叫你去按「設為機密卡片」。</span>
