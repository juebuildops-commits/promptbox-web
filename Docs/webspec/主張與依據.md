# 主張與依據（Claims & Evidence）

> **用途**：官網上每一句**對外主張**，配一條**可查證的依據**與**目前狀態**。
> **建立**：2026-08-28　·　**最後查核**：2026-08-30（§二之六 2026-09-15 新增）
> **為什麼需要它**：F11（「開源發布於 GitHub Releases」）、「也提供 macOS 版」、
> 「校驗碼供您核對**來源**」、「提供企業發票」—— 這四個問題全部是**事後一個一個撞出來的**，
> 而且是同一種失效模式：**頁面上有一句話，而它的依據不成立或已過期**。這張表把那件事變成可檢查的。
>
> **上位原則**：誠實紅線 C1 / C2 —— 誠實是紅線，不是美德。
> 「不得在任何介面或文案暗示超出實際的保護」「已知邊界必須明列，不得模糊」。
> 全文見 [App 事實依據 §二](../appsync/App事實依據.md)。

---

## 怎麼用

- **改文案之前**：先看這裡有沒有對應的列。沒有 ⇒ 你正在新增一條沒有依據的主張。
- **依據欄的三種寫法**，不可混用：
  - **連結** —— 依據就在這個 repo，點得開
  - **`App A5` 這種編號** —— 依據是 App 的行為，正本在 App repo，本 repo 只有 [App 事實依據](../appsync/App事實依據.md) 的快照
  - **「無」** —— **真的沒有依據**。不要為了讓表格好看而編一個連結出來
- **不必一次填完**。[PageDescription](./PageDescription/) 的「核心內容映射」是主張的來源清單，邊改邊補即可。

---

## 一、產品能力主張

> 本節的依據全部在 App 端。編號對應 [App 事實依據 §一](../appsync/App事實依據.md)，
> 🔴 **正本在 App repo，本 repo 沒有 ADR 副本**——需要完整脈絡時請自行去 App repo 取。
>
> ⚠️ **一個例外**：「首頁標題組換稿（2026-09-10）」那一列的依據在 [BRD-01 §2](../refer/BRD/BRD-01_定位與市場.md)，不在 App 端。
> 它是**定位主張**不是能力主張，暫放本節，因為本表還沒有「定位」一節 —— 下一次有第二條定位主張進來時再分家。

| 頁面主張 | 位置 | 依據 | 狀態 |
| --- | --- | --- | --- |
| 100% 完全不連網（Zero Outbound） | 首頁 `faq.a1`、下載頁 `notes.n1`、隱私頁 | **App A5**。護欄測試 `electron/__tests__/no-outbound.test.js`（7 條）擋回加 | ✅ |
| 唯一的網路元件是內建 MCP 伺服器，**只監聽 `127.0.0.1`**、不對外連線 | 首頁 `faq.a1`、下載頁 `notes.n1`、隱私頁 `appVsSite`、[手冊第 7 章](../../Src/content/docs/7.mcp.md) | **BRD-03 §9 規則 6**：講「不連網」時若不一併說明本機 listener，會被技術使用者抓語病 —— **主動說反而加分** | ✅ 2026-08-30 補上前三處（原本只有手冊有，而主張講在另外三頁） |
| **SQLite + ChaCha20** 整庫靜態加密，金鑰由 DPAPI / Keychain 保護（~~SQLCipher 頁級靜態加密~~、~~SQLCipher（AES-256）~~） | 下載頁 `notes.n2`、步驟說明、首頁 `faq.a1`／`faq.a3`、meta、信任列、隱私區塊、`b1`、`f6`；定價頁 `f6`／`r8`；隱私頁；企業頁 `trust.i2`；手冊第 1 章；關於頁（三語） | **App A1**（安全是產品本體）＋ App repo `electron/platform/dbCipher.cjs` 明設 `cipher: 'chacha20'`（SQLite3 Multiple Ciphers 的 ChaCha20-Poly1305）；以 `sqlcipher`／`aes256cbc` 重開同一檔案皆 `file is not a database`（App v3.10.0 TD §1.1-A）。創辦人 2026-09-18 查核確認 | ✅ **2026-09-18 全站更正（46 處）**，見 [WL-006 §2-5](../Wishlist/006.WL_mac暫停下載與Windows換新版.md)。🔴 **「SQLCipher」從 v3.0.0 起就是錯的**：App 之前只下 `key`、方案由函式庫預設值決定，而預設一直是 chacha20；v3.10.0 只是把它明設。**對外只寫方案名、不寫版本號**（函式庫每次升級版本都會變） |
| ✅ **首頁標題組定案為「AI 指令保險庫」（2026-09-14）**：H1「專屬你的 AI 指令保險庫」＋副標「將每一次成功的 AI 溝通，轉化為隨時可用的專屬樣板」 | 首頁 `hero.titleLead`／`titleAccent`／`lead` | [BRD-01 §2](../refer/BRD/BRD-01_定位與市場.md)（以「保險庫 Vault」建立安全價值與 $39 價格心理錨定）＋ [BRD-00 #16](../refer/BRD/BRD-00_索引與決策狀態.md) | ✅ **2026-09-14 已回寫正本並結案**：從「收納櫃」演進為「指令保險庫」，BRD-01 與 BRD-00 決策 #16 已同步更新，價格心理錨定與工作流複用效益已閉環對齊 |
| ✅ **「採用 SQLite + ChaCha20 與 AES-256 雙層加密技術」**（~~SQLCipher 與 AES-256~~，2026-09-18 更正） | 首頁 `hero.body`（2026-09-14 改寫） | **App A1**（SQLite + ChaCha20 全庫靜態加密，見上方第一列依據）＋ **App A3**（機密卡 scrypt + AES-256-GCM 加密）＋ [首頁 privacy.item1](../../Src/i18n/locales/zh-TW/home.json) | ✅ **2026-09-14 改寫**：與首頁隱私區塊「雙層加密」完全對齊 |
| ✅ **「你的 Context 存在你自己的電腦，任何廠商都拿不走」**（2026-09-14 改寫） | 首頁 `hero.body` | **App A5** 零主動外連（存放於本機）＋ [BRD-01 §2 / §5](../refer/BRD/BRD-01_定位與市場.md) 中立護城河 | ✅ **2026-09-14 改寫結案**：原 09-10 版「放心地把公司報告做成提問格式，無須擔憂雲端外洩」因涵蓋「使用時送給 AI」而有 C1 踩線疑慮；新稿主詞嚴格收回「存放（存在你自己的電腦）」，完全符合技術事實，C1 懸案解除 |
| 機密卡片 scrypt + AES-256-GCM，AI 取用須人類授權才釋放 | 首頁 `faq.a2`、[手冊第 5 章](../../Src/content/docs/5.secrets.md) | **App A3 / A4**（機密預設不自動吐出、密鑰威脅模型） | ✅ |
| 授權時可勾「10 分鐘內不再問我」，該期間不重複詢問（純記憶體、不快取密碼、鎖屏即失效） | 首頁 `faq.a2`、手冊第 5 章 | v3.6.0 行為，[changelog v3.6.0](../../Src/content/changelog/v3.6.0.md) | ✅ 2026-08-30 補進首頁（原本 FAQ 只寫「單次釋放」，讀起來像每次都要重新授權） |
| 🔴 **揭露防護下降：解鎖後切到別的視窗不會自動上鎖，明文最長留 10 分鐘** | [手冊第 5 章](../../Src/content/docs/5.secrets.md) 的 `notice-warning` | **[App 事實依據 §二 C2 第 5 條](../appsync/App事實依據.md)**（v3.8.0 FR-8／ADR-009）＋ **BRD-03 §9 規則 8**（2026-09-06 新增）。誠實紅線 **C2「已知邊界必須明列」** | ✅ **2026-09-06 首次揭露**（創辦人同日裁示要寫）。<br>🔴 **這是一條「壞消息」主張，比 v3.6.0～v3.7.2 的保護更弱**：blur 三次被證明是錯的代理指標，第三次選擇移除觸發源而不是再開一個例外。仍然有效的失效條件：idle 10 分鐘／鎖屏／睡眠／手動撤銷。<br>⚠️ 講「閒置自動鎖」時**不得略過這一條**，否則讀者會以為切走視窗就安全了 |
| 🟢 **串聯卡片裡就地解鎖的機密，現在也吃「閒置 10 分鐘自動鎖回」** | 首頁 `faq.a2`、[手冊第 5 章](../../Src/content/docs/5.secrets.md)、[`/about` 2.how](../../Src/content/about/2.how.md)、[changelog v3.9.0](../../Src/content/changelog/v3.9.0.md) | **[App V390-2](../appsync/App事實依據.md)**（v3.9.0 FR-3） | ✅ **2026-09-10 落檔**。🔴 **這一列的重點是「官網那句話此前並不完全成立」**：先前只有直接解鎖的機密卡會自動鎖回，串聯裡那一份的計時器**從未被啟動過** —— 而官網從來沒有把情境講細，讀起來像全部都會鎖。<br>⚠️ **不解除上一列**：blur 仍然不是失效條件，兩條要一起講 |
| 🔴 **「要備份就複製整個資料夾；只複製 `vault.db` 與 `keystore.bin` 是解不開的；還原只確認在同一個 Windows 帳戶內有效，換電腦尚未驗證」**（2026-09-18 新增） | [手冊第 8 章](../../Src/content/docs/8.settings.md)「重設資料提醒」、[`/about` 3.status](../../Src/content/about/3.status.md) 的 `notice-warning`、[`/about` 2.how](../../Src/content/about/2.how.md)「金鑰保管」列、企業頁 `trust.i3`（三語） | [App 側回覆 3](../appsync/App側回覆-3.md) **§三**（W9，2026-09-18 v3.10.0 FR-6 探針實測）：Windows 的 `safeStorage` 走 Chromium OSCrypt，DPAPI 保護的是 `userData/Local State` 裡的金鑰，那把金鑰才解 `keystore.bin` ⇒ **同一個 Windows 帳戶 ＋ 那個 userData 的 `Local State`** 缺一不可。同機同帳戶、只帶 `keystore.bin` ⇒ 實測解不開。誠實紅線 **C2** | ✅ **前兩句是實測，可以寫死**。🔴 **第三句只能寫「尚未驗證」**：換機／換帳戶那一層本次沒測（App repo WL-054 未結案）—— 與下一列同一個限制，**不得寫成「換電腦也能還原」**。📌 覆蓋安裝不受影響：NSIS 不動 userData |
| 🔴 **換機／重灌後整個資料庫（含一般卡）可能開不起來，且軟體沒有匯出／備份功能** | [`/about` 3.status](../../Src/content/about/3.status.md) 的 `notice-warning`（原本全站唯一一處）＋ [changelog v3.9.1](../../Src/content/changelog/v3.9.1.md)（2026-09-10 新增第二處） | **[App V391-3](../appsync/App事實依據.md)**（v3.9.1 設密碼面板的 `warnLocalOnly`）。誠實紅線 **C2** | 🟡 **依據強度有上限，措辭因此被限制**：App 側自承這句是**從機制推導、尚未真機驗證**，待辦留在 App repo 的 WL-054（未結案）⇒ 官網一律寫「**可能／不保證**」，**不得寫成「一定無法開啟」**。<br>📌 **風險方向是我們可能誇大了**，不是說少了 —— 這與本表其他列相反，改稿時不要順手「加強語氣」 |
| ✅ **「AI 無法透過串聯間接讀取『對 AI 隱藏』的卡片或機密卡片」＋「你自己的預覽裡，隱藏卡同樣展不開；機密卡可就地解鎖、解鎖後可展開與複製——這條隔離擋的是 AI，不是你」**（2026-09-12 改寫） | [手冊第 6 章](../../Src/content/docs/6.chaining.md) 串聯特性第三點 | **[App V394-3](../appsync/App事實依據.md)**（2026-09-12 對 v3.9.4 原始碼查核：MCP 端兩者皆拒絕展開、無 consent 路徑；GUI 預覽 hidden 拒絕、secret 轉就地解鎖） | ✅ **缺口 8 於 2026-09-12 結案**：照下方原列的建議「把主詞加回去，不是刪掉這句」，並把「對使用者不成立」的那一半明寫出來（C2）。原句的「鎖定卡」一併改掉（缺口 10）。<br>⬇️ 以下為原列，保留供對照 |
| ~~「鎖定卡與機密卡無法被串聯間接讀取，防止機密旁路洩漏」~~ | [手冊第 6 章](../../Src/content/docs/6.chaining.md) 串聯特性第三點（**2026-09-12 前**） | 🔴 **無 —— 本表原本就沒有這一列**，而 v3.9.0／v3.9.2 的行為與它牴觸：串聯卡可以引用機密卡，使用者**就地輸入密碼解鎖**後明文會展開進預覽，[v3.8.1 起可整段複製](../../Src/content/changelog/v3.8.1.md)，[v3.9.2 起連機密卡自己的變數值都會代入](../../Src/content/changelog/v3.9.2.md) | 🔴 **缺口（見下方第 8 條），2026-09-10 補版本紀錄時發現**。<br>最寬容的讀法是「**AI** 無法經串聯間接讀取」—— 那句話成立（機密內容仍須 consent）。但這一句**沒有主詞** ⇒ 讀起來像「串聯裡看不到機密卡的內容」，那是 **C1**（暗示超出實際的保護）。<br>📌 **修法是把主詞加回去，不是刪掉這句** —— 隔離對 AI 那一側是真的 |
| ✅ **「一鍵『對 AI 隱藏』：關掉眼睛後，AI 連這張卡存在都不知道」**（2026-09-12 前為「卡片一鍵鎖定；…」） | 首頁 `home.advanced.row1.b3`（三語，按鈕名用 App 原文：「對 AI 隱藏」／"Hide from AI"／「AI から隠す」）；手冊第 5 章 navLabel 同日由「機密卡片與資料鎖」改為「機密卡片與 AI 可見性」 | 後半句：v3.6.0，關掉眼睛的卡不進 `tools/list`（§二之四「對 AI 隱藏之後」那列同源）。<br>前半句：**[App V394-2](../appsync/App事實依據.md)** —— App 自 v3.9.4 起**不再把「對 AI 隱藏」叫成「鎖」**，「鎖」只剩「有密碼」一個意思 | ✅ **缺口 10 於 2026-09-12 結案**（同日發現、同日修）。本表原本沒有這一列（§二之四 那列說「與 §一 同源」，但 §一 從來沒有它）。<br>📌 **v3.0.0 條目的「資料鎖」「未鎖定卡片」不改**，那是當時的名字。首頁 `imageAria`／`imageAlt` 的「鎖定狀態」指的是機密卡的密碼鎖，**用法正確，不改** |
| 配額提示在**三項卡片配額任一**用滿時出現；機密卡撞額度**一律彈窗說明原因**（不因關閉升級提示而改為行內文字）。附帶自承：關閉升級提示時，原本那句行內說明**從未顯示過** | [changelog v3.9.3](../../Src/content/changelog/v3.9.3.md) | **[App V393-1／V393-2](../appsync/App事實依據.md)** | ✅ App 行為成立，**BRD 正本 2026-09-12 已同步**：[BRD-03 §4.0-b／§4.0-c／§4.3](../refer/BRD/BRD-03_授權與Free-Pro邊界.md)、[BRD-00 決策 #26 改寫、#28 新增](../refer/BRD/BRD-00_索引與決策狀態.md)（缺口 11 結案）。<br>📌 自承的那一則性質是 UX 而非保護，**不算更新日誌上的第三筆「壞消息」**（第一、二筆是 v3.8.0、v3.9.1） |
| 🔴 **密碼保護的是卡片內容與變數（名稱與預設值）；「不」受保護的是卡片的名稱與說明** | 首頁 `faq.a2`、手冊第 5 章邊界告示、`/enterprise` `trust.i2`、`/about`（原 `/what-we-dont-do` 職能承接） | **[App V38-1／V38-2／V38-3](../appsync/App事實依據.md)**（v3.8.0 三份密文：`content` / `variables_enc` / `schema_def_enc`）＋ **BRD-03 §9 規則 1／2**（已於同日加註範圍收窄）。<br>🔴 **規則 7 與 ADR-006 已被 v3.8.0 推翻，不得再引用** —— 它們的前提是「機密卡的參數名以明文存在 DB 快照欄位」，而該欄位（`secret_var_names`）已隨 schema 刪除 | ✅ **2026-09-06 改口**。此前寫「卡片名稱、說明、變數名稱與變數預設值都不受保護」，那是 v3.7.1 的實況。<br>⚠️ **改口的同時必須保留 V38-2 那半句**：一般卡（沒設密碼的）變數值仍是明文，**且是永久邊界**——沒有密碼就沒有金鑰。少了它，讀者會以為所有卡片的變數值都被保護了 |
| 升級覆蓋安裝，安裝路徑不變、資料原地保留 | 下載頁 `notes.n1`、首頁 `faq.a3`（2026-08-31 起不再包含 `notes.n3`／v2.x 升級敘述） | **App B5** 資料連續性紅線；[身分字串凍結清單](../appsync/身分字串凍結清單.md) | ⚠️ 原則成立，但**護欄只覆蓋一半**——見 §缺口 4 |
| 核心功能永久免費、配額只擋新增不刪既有 | 首頁 `faq.a5`、定價頁 | **App A2**（安全能力永不成為付費牆）、[FP04 §四](../refer/FP04.md) | ⚠️ 具體配額數字見 §缺口 3 |
| 🔴 **45 是「卡片總數」，機密卡與串聯卡都算在內**（不是 45+3+5=53） | 首頁 `price.free.f2`／`faq.a5`、定價頁 Free 卡與對照表 r2–r4 | [BRD-03 §2.0](../refer/BRD/BRD-03_授權與Free-Pro邊界.md) 總數口徑定案；§4.0-b2 在 App 端明令標籤不得寫成會被相加的形式 | ✅ **2026-08-30 改為總數口徑**。此前四處把三個數字平行並列，正是 §4.0-b2 要避免的誤讀 |
| 支援 Windows 10/11、MCP 接 Cursor / VS Code / Claude Desktop 等 | 首頁 `faq.a4`、下載頁 | [App README 快照](../refer/APP快照.md)（v3.7.1） | ✅ |
| 🔴 **Free 完整擁有：全庫靜態加密（永久免費、不設限）、變數樣板與變數替換、機密釋放授權窗與閒置自動鎖、釋放稽核的紀錄與檢視** | 首頁與定價頁的 Free 卡、定價頁對照表 r7／r8／r13 | [BRD-03 §1 G1](../refer/BRD/BRD-03_授權與Free-Pro邊界.md)（加密永久免費，對外口徑必須是「加密永遠免費；Pro 賣的是規模與釋放護欄」）、§2 真相表、§2.2（稽核記錄與檢視不 gate）、§2.5（授權窗與閒置自動鎖不 gate）、§2.6（變數替換不 gate） | ✅ **2026-08-30 首次上頁**。此前定價頁的免費卡從頭到尾沒有「加密」兩個字，等於 G1 規定的對外口徑一次都沒被說出來 |
| 🔴 **Pro 相對 Free 只多兩件事：解除四項配額 + 釋放稽核匯出** | 首頁與定價頁的 Pro 卡、對照表 r14 | **BRD-03 §9 規則 3** 的正確口徑；§2.6 ② 強型別自 v3.7.0 **全面停用**，「停用期間不得再提強型別，升級頁與行銷文案一併移除」 | ⚠️ **稽核匯出尚未實作**（§2.2「唯一沒有的是匯出」、§7 P3 列為 Pro 上線工作項），頁面標示為「隨 Pro 版一同推出」。Pro 整體本就未上架（Build B 未做，[BRD-04 §5](../refer/BRD/BRD-04_版本路線與時序.md)），四張卡的行動點都是登記表單 |
| ⛔ **已移除**：「動態變數樣板」與「資料匯出／匯入（JSON／CSV）」曾被列為 Pro 專屬 | 原 `pricing.pro.f5`／`f6`、`home.price.pro.f4`／`f5`、對照表 r7／r8 | **無依據**。變數在 BRD-03 §2 是「無限｜不 gate」，§2.6 明文「gate 它等於 gate 變數」；資料匯出／匯入**不在真相表上**。若照原文案實作＝[BRD-04 §5.1](../refer/BRD/BRD-04_版本路線與時序.md) 最忌的 clawback（收回已免費的功能） | ✅ 2026-08-30 移除（D1 決策 a）。**留下這一列是刻意的** —— 下一個人看到 Pro 賣點變薄時，要能一次讀到它為什麼變薄 |

## 二、發布與信任主張

| 頁面主張 | 位置 | 依據 | 狀態 |
| --- | --- | --- | --- |
| 安裝檔由官網直接發布 | 下載頁 `hero.lead`、`trust.t1` | `Src/shared/downloads.ts` 的 R2 常數（2026-09-15 前在 `download.vue`）；[上線前置鏈 P1](../webplan/上線前置鏈.md) | ✅ 2026-08-28 起由 Cloudflare R2 發布 |
| 附 SHA-256 校驗碼，可核對**檔案完整性** | 下載頁 `checksum.*`、首頁 `faq.a3` | `Src/shared/downloads.ts` 的 `sha256` 常數（2026-09-15 前在 `download.vue`）。2026-08-28（v3.7.1）以 R2 ETag（單段上傳＝物件 MD5）驗證線上檔案與本機產物位元組相同。<br>🔴 **2026-09-10（v3.9.2）改用更強的做法**：兩顆檔案**整顆下載回來實算 SHA-256** 與頁面上要印的值比對，兩顆皆相符。<br>⚠️ **ETag 比對不足以支撐這一列**：ETag 是 MD5，與頁面印的 SHA-256 是兩種雜湊 —— 它證明得了「線上檔案＝本機產物」，證明不了「印出去的那串校驗碼是對的」 | ✅ |
| 🔴 **尚未購買程式碼簽章憑證**，Windows 會顯示「無法辨識的應用程式」 | 下載頁 `notes.n4`、[手冊第 9 節](../../Src/content/docs/9.install-safety.md) | [上線前置鏈 **P5**](../webplan/上線前置鏈.md) | ✅ **已誠實揭露**（依誠實紅線 C2「已知邊界必須明列」） |
| ~~macOS 10.15+ 原生支援、Apple Silicon 與 Intel 雙架構~~ | ~~下載頁 mac 卡片；首頁 CTA；首頁 `trust.platforms`／`faq.a4`、下載頁 `meta.*`~~ | 2026-08-30：**無 build** ⇒ 改為「即將推出」。2026-09-14 build 上架時卡片被改成下載鈕，但「10.15+」沒改、其餘五處仍寫「即將推出」 | ❌ **2026-09-15 由下兩列取代**。「10.15」與「Intel 雙架構」**兩句都不成立**（見下一列依據） |
| ✅ **macOS（Apple Silicon）版可下載**：需 **macOS 13（Ventura）以上**、僅 arm64、Intel Mac 無法執行、金鑰由 macOS Keychain 保管 | 下載頁 mac 卡片（`platforms.mac.*`）、`meta.*`、`platforms.lead`、`desktopOnly.note`；首頁 CTA ×2（`common.cta.downloadMac`）、`trust.platforms`、`faq.a4`；關於頁 `3.status.md` | R2 上的 `PromptBox-3.10.0-arm64.dmg`（`Src/shared/downloads.ts`；SHA-256 **2026-09-27 由 Claude 整顆下載實算**，該檔 MD5 與 R2 回的 ETag 相符 ⇒ 實算的就是使用者會拿到的位元組序列，⏳ **待創辦人自行比對**）。**最低版本 macOS 13**：直接取自這顆 dmg 的 `PromptBox.app/Contents/Info.plist`，`LSMinimumSystemVersion = 13.0`（2026-09-27 解開安裝檔讀出）；成因是 App 用 Electron 44，[Electron Breaking Changes](https://www.electronjs.org/docs/latest/breaking-changes) 44.0 起不支援 macOS 12（38.0 起不支援 11、33.0 起不支援 10.15）。**架構**：R2 只有 arm64 一顆。**Keychain**：App 以 Electron `safeStorage` 保管 DB 金鑰（[SYNC-001](../appsync/SYNC-001_名詞對照表.md) `safeStorage` 列） | ✅ **2026-09-15 全站改口**（創辦人裁示維持上架，供測試人員跑完整流程）。⚠️ App 升到 Electron 44 時最低版本會變 macOS 13，要回來改。<br>⏸ ~~**2026-09-18～09-27 暫停**（[WL-006](../Wishlist/006.WL_mac暫停下載與Windows換新版.md)）：v3.10.0 沒有 mac build ⇒ `downloads.ts` 拿掉 `mac`、mac 卡按鈕停用、會員專區拿掉 mac 卡；暫停期間其餘 mac 文案依創辦人裁示刻意不改（WL-006 §2-1）~~<br>✅ **2026-09-27 重新上架**（WL-006 §2-3）：v3.10.0 的 dmg 發佈到 R2，三處一起加回，暫停期間欠的文案同批收回 —— **最低需求由 macOS 12 改為 13**（下載頁 `meta.description`／`platforms.mac.requirement`、首頁 `faq.a4`、手冊第 9 節、關於頁 `3.status.md`，三語共 8 處），手冊的 `shasum` 範例檔名一併更正為 `3.10.0`。<br>🔴 **macOS 13 這個數字沒有任何護欄在守**：App 的 `package.json` 未設 `build.mac.minimumSystemVersion`（App 側回覆 3 W8 未做）⇒ 值來自 Electron 預設，**升大版會無聲改變**；`check:release` 只看版號。換 Electron 後必須重讀 dmg 的 `Info.plist` |
| 🔴 **macOS 版沒有 Apple 開發者簽章、沒有公證，第一次開啟會被 macOS 擋下** | 下載頁 `notes.n5`、首頁 `faq.a3`／`faq.a4`、[手冊第 9 節](../../Src/content/docs/9.install-safety.md)「四、（macOS）」 | [上線前置鏈 **P4**](../webplan/上線前置鏈.md) 未啟動（沒有 Apple Developer 帳號 ⇒ 不可能公證）；App `package.json` 的 electron-builder 設定**沒有 `mac` 區段**（無簽章身分、無 notarize）。放行方式引 [Apple 官方說明](https://support.apple.com/zh-tw/guide/mac-help/mh40616/mac) | ✅ **2026-09-15 揭露**，與上面 Windows 那列同一個框架：解釋警告在說什麼、先核對校驗碼、**不教人用終端機繞過**。⚠️ 實際跳出的字句依 macOS 版本而異，手冊刻意不轉述操作步驟，只連到 Apple |
| 🔴 **「macOS 版覆蓋安裝新版之後，資料是否完整延續，還沒在 Mac 上實測過」** | 下載頁 `notes.n5`、首頁 `faq.a3` 括號、手冊第 9 節 notice、關於頁 `3.status.md` | [App 事實依據](../appsync/App事實依據.md)「macOS 仍未驗」；[SYNC-000 §3.2](../appsync/SYNC-000_索引與雙方進度.md)「macOS 實機驗收」🧪 未做。`app.setName('Vault')` 已在 App 程式碼與單元測試裡（SYNC-000 G1），但**沒有實機** | ✅ **C2 的執行**。🔴 **實機驗收通過之前不得拿掉**；通過後四處一起改 |
| 🔴 **手機與平板不給下載鈕**：PromptBox 是 Electron 桌面程式，手機／平板裝不起來 | 下載頁 `desktopOnly.cta`／`note`／`cardNote`（hero 與 Windows 卡片，視窗 <1024px） | [APP快照 §系統需求](../refer/APP快照.md)：「Windows 10/11 或 macOS 10.15+」、「**Desktop**: Electron」。與上一列同一條原則 —— **下載不到東西的按鈕，比誠實說做不到傷害大** | ✅ **2026-09-10 上線**（創辦人裁示）。<br>🔴 **判定是視窗寬度（`lg:` = 1024px），不是裝置偵測** —— 兩案的誤判方向相反，創辦人在知情下選了寬度：桌機拉窄視窗**會被誤擋**、iPad 桌面版網站（≥1024px）**擋不到**。<br>⚠️ **`note` 的第二句「把視窗拉寬，下載按鈕就會出現」自己也是一條主張**，依據是 `download.vue` 的 `hidden lg:inline-flex`。它同時是上面那個誤判**唯一的出路** —— 哪天改成 UA／`maxTouchPoints` 偵測，**必須同一次把那句話改掉**，否則它會變成一句假話 |
| 本站未載入任何分析工具 | 隱私頁 | `NUXT_PUBLIC_GTAG_ID` 留空 ⇒ 完全不注入 gtag（D4） | ✅ |
| 隱私頁「本站不使用 cookie 做任何個人化或追蹤」只在 GA4 與會員登入**都沒設定**時顯示；任一個設定了，改為「不使用 cookie 做個人化或廣告」並逐項列出 cookie。GA4 那一項：`_ga`、`_ga_*`，用來分辨回訪的瀏覽器，只在分析開著時寫入 | 隱私頁 `storage.body`／`bodyWithCookies`／`gaCookie`（條件式） | GA4 的 gtag.js 預設寫入第一方 cookie `_ga` 與 `_ga_<ID>`；`nuxt.config.ts` 的 `gtag.config` **沒有**設 `client_storage: 'none'`。退出分析 ⇒ `plugins/analytics.client.ts` 不載入 gtag.js ⇒ 不會再寫入（已存在的要清瀏覽器資料） | ⏳ GA4 未設定，目前顯示的是原句（成立）。🔴 **2026-09-15 前的版本在填上 `NUXT_PUBLIC_GTAG_ID` 那一刻就會變成假話** |
| 電子報**不使用開信追蹤像素** | 隱私頁；**訂閱表單 `subscribe.privacyNote`（現為 5 頁：首頁／download／pricing／changelog／privacy）** | D29 選 Buttondown 的理由；需在 Buttondown 後台**實際關閉**（[D30 上線步驟 2](../webplan/待討論問題.md)） | ✅ **2026-08-30 已兌現**：帳號已建立、開信追蹤已於後台關閉（創辦人確認） |
| 訂閱信箱**不轉售、不共用，可隨時一鍵退訂** | 訂閱表單 `subscribe.privacyNote`（5 頁） | 無第三方接收者：`server/api/subscribe.post.ts` 只呼叫 Buttondown 一個端點；退訂由 Buttondown 提供 | ✅ 2026-08-30 起 Buttondown 已部署 |
| 送給 Buttondown 的個資**只有使用者主動提供的 email**（不含 IP） | 隱私頁第三方處理者矩陣；`subscribe.privacyNote` | 🔴 **路線 B**（2026-08-30 裁示）：`subscribe.post.ts` 刻意**不轉發訪客 IP**，即使 Buttondown 官方建議這樣做可避免防火牆誤判。代價由我方限流承擔 | ✅ 已實作。**改動此決定必須同時改隱私頁與本表**（程式檔頭已寫明） |
| 訂閱表單的**失敗訊息陳述的是真實原因**（`subscribe.state.*`） | 5 頁訂閱表單 | `subscribe.post.ts` 的分支與 HTTP 狀態碼一一對應；`blocked` 分支現有 `console.error` 記錄 `firewall_reasons` | ⚠️ **2026-08-30 修正過一次**。`blocked` 原本斷言「含 `+` 的別名信箱常遇到」＋「重送同一個不會有不同結果」——**兩句都是從一次觀察推出的，第一句已被實測推翻**（見 [F17](../webplan/待討論問題.md)）。現行文案**不對原因、也不對可重試性表態** |
| App 自 v3.3.0 起完全不連網，**永不主動檢查更新**，故 email 是唯一的新版通知管道 | 訂閱表單 `subscribe.updates.body`（首頁 2026-08-30 起也顯示此句）；D20 | **App A5** 零外連（同 §一 第 1 列）；[APP快照](../refer/APP快照.md) 的 v3.3.0 版本歷程 | ✅ |
| 🔴 **主動揭露一則曾經失效的保護**：v3.9.1 之前按「設為機密卡片」，加密的是資料庫裡的舊內容，剛貼上的金鑰從未被寫入、更未被保護 | [changelog v3.9.1](../../Src/content/changelog/v3.9.1.md) | **[App V391-1](../appsync/App事實依據.md)**。誠實紅線 **C1**（介面做出了保護的樣子，而保護作用在別的資料上）＋ **C2** | ✅ **2026-09-10 寫上頁面。這是更新日誌上第二筆刻意寫進壞消息的紀錄**（第一筆是 v3.8.0 的 blur 防護下降）。<br>📌 **首發前無使用者** ⇒ 它揭露的不是一則事故通知，而是「這個產品怎麼處理自己的錯誤」，那正是可驗證性這條獲客路徑的素材 |
| ~~「v3.9.2 現已推出」~~ → **2026-09-12 由下一列取代** | ~~changelog `meta.description`／`cta.body`、下載頁 `meta.description`／`hero.badge`（三語，2026-09-10）~~ | `download.vue` 的 `WIN_EXE`／`WIN_ZIP` 指向 R2 `/V3.9.2/` 的兩顆實體檔案，SHA-256 **整顆下載實算比對通過**（2026-09-10） | ✅ **當日結案（缺口 7）**。此前日誌宣稱 v3.9.2 而下載頁給 v3.7.1，相差五個版本。<br>📌 **留這一列是刻意的**：日誌與下載頁是兩處分開更新的地方，落差出現過一次就會出現第二次 |
| ✅ **「v3.10.0 現已推出」**（~~v3.9.4，2026-09-12~~） | changelog `meta.description`／`cta.body`、下載頁 `meta.description`／`hero.badge`（三語，2026-09-18）＋ 更新日誌的「最新版本」徽章落在 [v3.10.0 條目](../../Src/content/changelog/v3.10.0.md) | `shared/downloads.ts` 的 `win`／`win-zip` 指向 R2 `/V3.10.0/` 的兩顆實體檔案（HEAD 皆 200，大小與 App 建置產物逐位元組相符）。SHA-256 **2026-09-18 由 Claude 整顆下載 R2 檔案實算**，⏳ **待創辦人比對**。`mac` **2026-09-27 起也指向 `/V3.10.0/` 的 dmg**（見上方 macOS 列） | ✅ **當日結案（缺口 9）**。補日誌當下 R2 上查無 `/V3.9.3/`、`/V3.9.4/`（HEAD 四個皆 404），同日安裝檔上架、`href` 與 `sha256` 成對更新。<br>📌 **v3.9.3 沒有獨立安裝檔**：R2 直接從 v3.9.2 跳到 v3.9.4，日誌上的 v3.9.3 只以 v3.9.4 的形式可下載 |

> ✅ **2026-08-30 結案**：Buttondown 已成功部署、開信追蹤已在後台關閉（創辦人確認）。
> 上面三列的共同前提（`NUXT_BUTTONDOWN_API_KEY` 未設定 ⇒ 端點回 501）已經解除，
> 五頁的訂閱表單現在是真的會收人的。
>
> ✅ **F18 已於同日結案（2026-08-30 創辦人裁示）**：授權 email 由使用者**購買時確認，
> 預設帶入早鳥登記的信箱**。因此訂閱表單「這個信箱也是日後授權憑證上簽的那一個」
> **成立** —— 前提是購買流程要以它為預設值，這一條已寫進
> [待討論問題 F18](../webplan/待討論問題.md) 與
> [接縫文件](../appsync/官網金流與授權發放接縫.md)。
> 憑證綁 email 本來就是 [ADR-008](../appsync/ADR-008_授權憑證格式與離線驗證契約.md) / D12 早已定的事。

> 🔴 **2026-08-30 補正：這條裁決原本只套用在下載頁，首頁漏了。**
>
> 首頁四顆 CTA 有兩顆寫著「免費下載 macOS 版」（`common.cta.downloadMac`），
> 而 macOS build 不存在 —— 依 §上位原則的誠實紅線 C1，那是**暗示了超出實際的結果**。
> 更糟的是它還是視覺主鈕（實心品牌色），能用的 Windows 反而是次要鈕。
>
> 已改為 `common.cta.macSoon`（「macOS 版即將推出」／"macOS coming soon"），
> 主鈕樣式讓給 Windows。舊 key `downloadMac` 已從三個語系移除（全站只有首頁在用）。
>
> **為什麼會漏**：Q15-b 清點的是「GitHub Releases」相關字串，
> 首頁那兩顆的問題是**承諾一個不存在的平台**，不在那次清點的關鍵字裡。
>
> 🔴 **同日第二次補正：CTA 改完之後，還有三處在用文字宣稱已支援。**
> 首頁 `trust.platforms`（「Windows ・ macOS」）、首頁 `faq.a4`（「以及 macOS 10.15 Catalina 以上，原生支援 Apple Silicon 與 Intel」）、
> 下載頁 `meta.title`／`meta.description`（「Windows / macOS」「支援 … macOS 10.15+」）。
> 同一個缺位的 build，站上一度有四種說法，其中 meta 還是搜尋結果直接顯示的那一句。
> **教訓與上一次相同**：按鈕改完不等於主張改完 —— 一個平台名稱會散在 CTA、信任列、FAQ 與 meta 四種載體。

## 二之二、「我們不做什麼」頁的主張（2026-08-30 新增，D5；2026-09-14 徹底刪除代碼與規格）

> 🔴 **2026-09-14 頁面徹底刪除（結案 webplan Q5 - 方案 1）**：
> 創辦人決策定案採取方案 1，徹底清理孤兒頁 `/what-we-dont-do`、三語系檔（`what-we-dont-do.json`）及獨立規格文件（原 `PageDescription 07`）。
> 原本下表的六條主動放棄清單與措辭禁令，對外完全由 **[§二之五](#二之五關於頁的主張2026-09-07-新增about-上線)（`/about`）** 承接與正本維護。
> 下表作為主張演進之歷史存檔保留，標明各項主張之來源依據與約束效力。

| 頁面主張 | 位置 | 依據 | 狀態 |
| --- | --- | --- | --- |
| 六條主動放棄清單（團隊協作／雲端同步當賣點／版本控制與評測／prompt 市集／廣告與資料變現／硬 DRM） | `features.i1`–`i6` | [BRD-01 §4](../refer/BRD/BRD-01_定位與市場.md) 的放棄清單，逐條對應、不增不減；[BRD-05 §5](../refer/BRD/BRD-05_行銷與獲客.md)「首發即上」 | ✅ |
| 🔴 **不說「PromptBox 是開源軟體」——「現在還沒有公開的 repo」** | `words.i1` | **BRD-03 §9 規則 4**（只能說「核心開源」）＋ [BRD-02 §7.3](../refer/BRD/BRD-02_商業模式與定價.md)（open-core 啟動點在第一筆營收之後）＋ [BRD-00 U9](../refer/BRD/BRD-00_索引與決策狀態.md)（界線衝突未結案） | ✅ **這是目前唯一誠實的寫法**。<br>⚠️ repo 真的公開的那一天，要同時改：這一格的文案、本列、PageDescription 07 的 §2 與 §3（會多一條外連） |
| **不承諾可重現建置**；校驗碼只證明「檔案與我們發布的一致」 | `words.i2` | [BRD-05 §4.2](../refer/BRD/BRD-05_行銷與獲客.md)「不要開出兌現不了的支票」；標準 binary 含閉源模組 | ✅ 與 §二「附 SHA-256 校驗碼」列互為表裡 —— 一列講校驗碼證明什麼，一列講它不證明什麼 |
| 不暗示 Pro 提供更強的加密；**一般卡片的**變數預設值在 Free 與 Pro 都是明文 | `words.i3` | **BRD-03 §9 規則 1／2**（2026-09-14 依 v3.8.0 整條改寫：依卡片型別分述、**不得只講其中一半**；規則 2 理由改為 A2 紅線，分界線是**有沒有設密碼**，不是 Free 還是 Pro）、§1 G1、**[App V38-2](../appsync/App事實依據.md)** | ✅ **2026-09-06 隨 v3.8.0 收窄**。要防的那句話（「Pro 提供更強的加密」）依然不能講，理由沒有變：機密卡的變數值在 Free 與 Pro 一樣受保護，一般卡的在 Free 與 Pro 一樣是明文 |
| 不暗示機密卡「設了密碼一切都藏起來」 | `words.i4` | **BRD-03 §9 規則 7**（2026-09-14 原規則已刪除、編號保留；留下的正是這條護欄：卡片名稱與說明仍在 `tools/list`，不得換成「一切都藏起來」） | ✅ 2026-09-14 複查：文案已同時講清受保護範圍與不受保護的名稱與說明，符合改寫後的規則 1 與 7 |
| 不單獨講「完全不連網」，必定同時說明 MCP 本機 listener | `words.i5` | **BRD-03 §9 規則 5／6** | ✅ 全站四處講「不連網」的地方現在都成對出現 |

## 二之三、「支持組織」頁的主張（2026-08-31 新增）

> 這一節整體對應 [PageDescription 08](./PageDescription/08_enterprise.md)。
> 這一頁是 §三「25 席可列名」那一列所掛的產品待辦的兌現物。
> 🔴 **它承載的未來承諾密度是全站最高的，而其中沒有一項今天可以兌現** ——
> 也因此它與 §二之二 一樣：**每一列都沒有機器在守**。

| 頁面主張 | 位置 | 依據 | 狀態 |
| --- | --- | --- | --- |
| 🔴 **「這面牆現在是空的，還沒有任何一家組織」** | `hero.*`、`wall.emptyTitle`／`emptyBody` | 事實：商業授權尚未開放購買（D3，定價頁商業卡的行動點是 disabled 的 `<button>`）。**這是本頁的主體內容，不是待填的坑** | ✅ |
| 這面牆不會出現：①沒有實際付費的公司標誌（含使用中／測試中／洽談中）②案例研究與推薦語 ③「受各大企業信賴」這類查不到數字的集合名詞 | `wall.never.i1`–`i3` | 誠實紅線 **C1**（不得暗示超出實際）在本頁的具體形態。三條都是**縮小自身主張**的自我約束，性質同 §二之二 的措辭禁令 | ✅ ⚠️ 第一次有公司表達興趣時，`i1` 會立刻變成一句擋路的話 —— 這是本頁最可能被悄悄拿掉的一列 |
| 列名的**唯一**條件是 25 席；沒有另外的贊助方案、沒有付費置頂、不分等級 | `wall.rule` | 創辦人 2026-08-30 裁示能撐住的只有「25 席以上可列名」一句。<br>🔴 對標對象 Obsidian 用六層寶石分級，**本站刻意不採用** —— 分級會是一個沒有依據的新承諾 | ✅ |
| 商業授權 ${commercial}／人／年，**純年費、不提供買斷** | `license.i2` | [BRD-02 §3 價格表](../refer/BRD/BRD-02_商業模式與定價.md)、§3.1（買斷等於告訴公司「付一次就永久合規」，會消滅第二年續購動機） | ⏳ 未開賣。與定價頁同一組數字，皆出自 `app/utils/pricing.ts` |
| 功能與個人版 Pro **完全相同**，商業授權不解鎖任何額外功能 | `license.i1`、`license.lead` | [BRD-02 §3.1](../refer/BRD/BRD-02_商業模式與定價.md)：它賣的是合規與支持開發，不是功能 | ⏳ |
| 買 4 席送 1 席（${commercialBundleTotal}／年 發 {commercialBundleSeats} 把獨立 key） | `license.i3` | [BRD-02 §3.2](../refer/BRD/BRD-02_商業模式與定價.md) 促銷表「✅ 首發做」 | ⏳ |
| 榮譽制：App 端不檢查到期、無機器碼綁定、無密碼狗；**到期後已安裝版本永久正常運作，不鎖不降級** | `license.i4`、`trust.i4` | [BRD-02 §3.1](../refer/BRD/BRD-02_商業模式與定價.md)＋[ADR-008「刻意不存在的欄位」](../appsync/ADR-008_授權憑證格式與離線驗證契約.md)（`exp`／`updatesUntil`／`maxVersion`／`machineId` 全數禁止） | ✅ 設計層面已保證（欄位不存在＝沒有人能加那一行判斷式） |
| **尚未開放購買**；不接受任何購買意向；發票與報帳方式待開放時一併公布 | `license.notOpenTitle`／`notOpenBody`、`meta.description` | D3 同一裁決（與定價頁商業卡**必須同進退**）；發票需統一編號，[前置鏈 P2](../webplan/上線前置鏈.md) 未完成 | ✅ 與 §缺口 1 同源 |
| 零主動外連，**且同句說明內建 MCP 只監聽 `127.0.0.1`** | `trust.i1` | **App A5** ＋ **BRD-03 §9 規則 5／6**。§一第 1／2 列的複述，**不是新主張** | ✅ 🔴 拆成兩條會讓第一條變成不完整的主張 |
| SQLite + ChaCha20（~~SQLCipher~~，2026-09-18 更正）+ DPAPI／Keychain、機密卡 scrypt + AES-256-GCM 需人類授權；**同段揭露卡片的名稱與說明不受密碼保護** | `trust.i2` | **App A1／A3／A4** ＋ **[App V38-1／V38-3](../appsync/App事實依據.md)** ＋ BRD-03 §9 規則 1／2。§一第 3／4／6 列的複述 | ✅ **2026-09-06 隨 v3.8.0 改口** |
| 無伺服器、無帳號、無租戶，**因此備份與裝置遺失的風險由客戶自己的 IT 流程承擔** | `trust.i3` | 架構事實（全站唯一 server route 是 `/api/subscribe`）；後半句是 C2「已知邊界必須明列」——**主動講出代價，不只講好處** | ✅<br>🔴 **2026-09-18 補一句備份方式**：IT 最可能做的檔案級備份（只備 `vault.db` ＋ `keystore.bin`）是解不開的 ⇒ 同段寫明「複製整個資料夾、只確認同一個 Windows 帳戶內有效、換電腦尚未驗證」。依據見 §一「要備份就複製整個資料夾」列。**「裝置遺失」這個情境正好是未驗證的那一格**，不得讓讀者以為備份能在新電腦還原 |
| 「開放購買時我們會主動聯絡你」 | `cta.body`、`license.notOpenBody` | 與 `subscribe.commercial.body` 既有的同一句承諾（定價頁與隱私頁已在用），管道是同一份 Buttondown 名單 | ⏳ 依賴商業授權真的開賣 |
| ⏳ **列名同意怎麼取得 —— 沒有裁示** | 頁面上**刻意沒有任何一句**描述列名流程 | **無**。使用公司名稱與商標需要對方授權，而「買了授權就自動被公開列名」對某些客戶是採購阻礙而非好處 | 🔴 **放上第一個標誌之前必須先有答案**，與發票、報帳同一批決定。在有裁示前不得在頁面上加描述流程的句子（見 [PageDescription 08 §4](./PageDescription/08_enterprise.md)） |

## 二之四、「模擬試用」頁的主張（2026-09-05 新增）

> 這一節整體對應 [PageDescription 09](./PageDescription/09_demo.md)。
> 🔴 **這一節存在的唯一理由，是 `public/demo-app/index.html` 被刻意凍結。**
> 那份 1857 行的示範裡有三處與 App 實情不符（見下表第一列）。創辦人 2026-09-05 決定**不修它**，
> 改由外殼那層標明「這是示意」。**因此第一列不是補充說明，是其餘一切成立的前提** ——
> 拿掉那句聲明，就必須回頭修 demo 本體，兩者只能二擇一。

| 頁面主張 | 位置 | 依據 | 狀態 |
| --- | --- | --- | --- |
| 🔴 **「畫面為互動示意，工具名稱、連接埠與連線細節經過簡化，與實際的 MCP 介面不同」** | `demo.disclaimer.title`／`body`（`/demo` 頁的 `notice-warning`）、首頁 `home.demo.note`（英文版另註介面語言） | 誠實紅線 **C1**。demo 內已知三處落差，依據 [APP快照](../refer/APP快照.md)：①工具名寫 `list_prompts()`／`get_prompt({id})`，實為 MCP 標準 `tools/list`／`tools/call`，每張未鎖定卡暴露為 `prompt_{id}`（v3.0.0）②寫死 `127.0.0.1:8765`，實際 port 由 `electron/portDetector.cjs` **動態偵測**（`/sse` 端點本身為真）③工具回傳區印 `variables: …`，但機密卡 `inputSchema` **恆空、連參數名都不列**（v3.7.0），且「變數替換」設定**預設關閉**、只管一般卡 | ✅ 🔴 **這句話是 demo 內容凍結的交換條件**（[webplan/模擬試用頁.md](../webplan/模擬試用頁.md) D7）。要刪它 ⇒ 先修 demo |
| 「此示範不做真加密，你輸入的密碼只存在這個分頁的記憶體裡」 | `demo.disclaimer.body`；demo 內部亦有兩處自述 | 事實：`demo-app/index.html` 的密碼以明文存在 JS 狀態並直接字串比對。C2「已知邊界必須明列」 | ✅ |
| 「全部在瀏覽器裡跑，不會送出任何東西」 | `demo.hero.lead` | 事實：demo 為零外部依賴的單檔 HTML，無 fetch／XHR／WebSocket；外殼頁亦不對它發任何請求 | ✅ |
| 眼睛＝存在性、鎖頭＝密碼是**兩條正交的軸**；「機密且隱藏」是合法狀態 | demo 互動本身（卡片可同時顯示兩個徽章） | [APP快照](../refer/APP快照.md) v3.6.0「全站不再一符二義」 | ✅ 與 §一第 6 列同源 |
| 機密卡**仍會出現在** AI 的清單裡（標記為需授權），內容才需要 consent | demo 的 `list_prompts` 輸出 | v3.6.0「`tools/list` 放寬存在性，`is_secret` 退出過濾條件；對 AI 可見的僅限名稱、描述、ID」 | ✅ |
| 對 AI 隱藏之後，**AI 端沒有任何線索知道那張卡存在** | demo 清單下方的說明行 | v3.6.0：可見性全交給 `is_locked`，關掉眼睛的卡不進 `tools/list` | ✅ 與 §一「關掉眼睛後 AI 連這張卡存在都不知道」同源 |
| 授權窗「10 分鐘內不再問我」：綁單卡與單一來源、鎖屏／睡眠／手動撤銷立即失效、**窗內每次取用仍會記錄** | demo 的 consent 彈窗與卡片上的倒數徽章 | v3.6.0 逐字（純牆鐘、不跨 token、絕不落盤；稽核以 `result='allow_window'` 區分）；[BRD-03 §1](../refer/BRD/BRD-03_授權與Free-Pro邊界.md) 第 42／90 行（授權窗與稽核記錄皆不 gate、Free 也有） | ✅ 與 §一第 5 列同源 |
| 「密碼從頭到尾沒有離開過你的機器」 | demo 授權成功後的 AI 回覆 | v3.6.0「只快取明文，不快取密碼或派生金鑰 —— 『密碼永不快取』這條不變式沒有破」 | ✅ |
| **未填的變數原樣保留 `{{name}}` 佔位符**，不會靜默替換成空字串 | demo 的複製與工具回傳兩條路徑 | [APP快照](../refer/APP快照.md) v3.7.0：該版特意修掉「替換成空字串會讓輸出安靜地少一段」 | ✅ |
| MCP 伺服器**預設不啟動**，要由使用者在 App 這一側按下開關 | demo 的初始狀態與 MCP 狀態條 | v3.5.0「MCP 不再無條件自動啟動（預設關）：token 是明文寫進 IDE 設定檔的，伺服器永遠在跑等於暴露窗永遠開著」 | ✅ 🔴 demo 把開關放在 App 側而非 IDE 側是同一個理由 —— 放 IDE 會演成「IDE 一鍵就連上」，那是假的 |
| 不連 MCP 也能用：複製貼上那條路徑**永遠可用** | demo 右側的貼上欄（未連線時仍可送出） | 事實（GUI 複製不經 MCP）；亦是 [BRD-03 §5](../refer/BRD/BRD-03_授權與Free-Pro邊界.md) 第 1 號風險「MCP 設定門檻會讓付費牆變成沒人走到的門」的直接回應 | ✅ |
| ⏳ **demo 未演示 Free 配額**（卡片總數 45／機密卡 3／串聯 5／Token 2） | demo 內沒有任何配額提示 | 不構成錯誤主張（沒說「無限」），但 [BRD-03 §2.1](../refer/BRD/BRD-03_授權與Free-Pro邊界.md) 指出「第 4 張機密卡的升級動機遠強於第 46 張」，而 demo 剛好有 4 張卡 | ⏳ **機會而非缺陷**：在「把第 4 張設為機密」時提示 Free 只有 3 張，可把示範接上 `/pricing`。要做 ⇒ 得先解除 D7 的內容凍結 |
| ⚠️ **英文外殼包中文 demo** | `/en/demo` 全頁；入口在 `/en` 首頁那一段 | 事實：demo 內文未 i18n（[webplan/模擬試用頁.md](../webplan/模擬試用頁.md) D5／D10）。英文 `disclaimer.body` 與 `home.demo.note` **各有一句明說介面為繁體中文** | ✅ 🔴 那兩句是這件事不構成誤導的**唯一**依據，翻譯時不得省略 |

## 二之五、「關於」頁的主張（2026-09-07 新增，`/about` 上線）

> 這一節整體對應 [PageDescription 10](./PageDescription/10_about.md)。決策正本：[webplan/關於我頁.md](../webplan/關於我頁.md)。
> 🔴 **本頁是全站主張密度最高的一頁，而它幾乎全部住在 `Src/content/about/*.md`** ——
> 不是語系檔（`check:i18n` 看不到）、不是 `.vue`（沒有 code review 會掃到文案）。
> `check:content` 只驗有沒有裸露的 `{{ }}`。**實質上沒有任何機器在守這一頁。**
>
> 🔴 **本頁是 §二之二 的職能繼承者。** 「我們不做什麼」2026-09-03 自頁尾下架後成為孤兒頁，
> 創辦人 2026-09-07 裁示「都做在 about 這頁」⇒ BRD-01 §4 的六條放棄清單與 BRD-03 §9 的兩條措辭禁令原本**改由本頁承載**。
> 🔴 **但同日稍後，作者把其中一部分隱藏了（見下方隱藏清單）** —— 承載關係因此只成立一半。
> **§二之二 那些列仍然有效**（頁面還在線上），但它們現在**沒有人看得到**；真正對外的是下表。
> ⚠️ 兩節之間沒有機器在同步，改一邊要記得看另一邊（[webplan Q5](../webplan/關於我頁.md)）。
>
> ⚠️ **全頁一律用「我」不用「我們」** —— [前置鏈 P2](../webplan/上線前置鏈.md) 公司登記未完成，用公司口吻＝**C2 踩線**。這不是文風。

### 🙈 隱藏清單（2026-09-07，作者）

上線當日作者把五處內容以 **HTML 註解**藏起來 —— **檔案裡還在，線上讀不到**。理由：**作者覺得這樣講出去太死**。

| 藏了什麼 | 位置 | 站上還有沒有別處在講 |
| --- | --- | --- |
| 邊界表「作業系統的金鑰保管功能不可用時，資料庫**退回不加密開啟**」（C2 第 2 條） | `2.how.md` | 🔴 **沒有。全站僅此一處** ⇒ 藏了等於這條邊界在官網上不存在 |
| 邊界表「離線可驗證與防破解不可兼得」（C2 第 3 條） | `2.how.md` | ✅ 定價頁、[支持組織頁](#二之三支持組織頁的主張2026-08-31-新增)、首頁的「榮譽制／接受一定比例被破解」 |
| 邊界表「解鎖後切視窗，明文留 **10 分鐘**」（C2 第 5 條） | `2.how.md` | ✅ [`/docs` 機密卡片節](../../Src/content/docs/5.secrets.md) |
| 整節「兩句我不會說的話」（不說開源、不說可重現建置） | `2.how.md` | ⚠️ 只剩 §二之二 的 `words.i1`／`i2`，而那一頁沒有入口 |
| 不做清單三條（市集抽成／版本控制與評測／硬 DRM） | `3.status.md` | ⚠️ 同上，只剩 §二之二 的 `features.i4`／`i3`／`i6` |

⇒ **邊界表由六列剩三列、不做清單由六條剩三條。下表已依現況改寫。**

| 頁面主張 | 位置 | 依據 | 狀態 |
| --- | --- | --- | --- |
| 「出去：零條／進來：兩條（你 + 同機的 AI 通道）／機密卡片的內容不會自己出去」 | `1.why.md`「所以它變成了一個保險庫」 | **App A5**（零主動外連）＋ **App A3／A4**（機密預設不自動吐出、釋放必留稽核） | ✅ §一第 1／5 列的第一人稱複述，不是新主張 |
| 🔴 **「早期的 PromptBox 每次啟動都會連上網檢查新版……我把整套自動更新的程式碼刪掉了」** | `1.why.md`「為什麼連自動更新檢查都拔掉」 | **App A5**。護欄測試 `electron/__tests__/no-outbound.test.js`（7 條）＋ CSP `connect-src`／`style-src`／`font-src` 皆為 `'self'` | ✅ **刻意保留「曾經有過、後來拿掉」的敘事**（[webplan D6](../webplan/關於我頁.md)）：版本號不寫，但「我做過然後刪掉」比「我從來沒做」可信，那是這段的全部力氣 |
| 「代價是**你不會知道有新版本**」，新版以 email 通知、自行覆蓋安裝、資料原地保留 | 同上 | **App B5** 後半（絕不鎖死既有本地資料）＋ 下載頁 `notes.n1` 同源 | ✅ C2 的執行：**主動講出代價**，不只講好處 |
| 🔴 **誠實但書**：「完全不連網」只指軟體不會**自己**連線；①MCP 在本機開對內窗口、**預設關閉** ②使用者點擊的外連由瀏覽器開啟 | `1.why.md` 結尾 `notice-warning` | **BRD-03 §9 規則 5／6**（不得單獨講「完全不連網」）＋ **App A5** 的例外口徑 | ✅ 🔴 **這一則不得與前面那句拆開**，與 §二之三 `trust.i1` 同一條規則 |
| 技術棧 8 列（Electron／Vue／**SQLite + ChaCha20**（~~SQLCipher~~，2026-09-18 更正）／scrypt + AES-256-GCM／作業系統保管金鑰（Windows 另綁 App 資料夾，2026-09-18 補）／MCP SDK／三語／字型隨軟體打包） | `2.how.md`「用了什麼」 | **App A1**（安全是產品本體）＋ [APP快照](../refer/APP快照.md)（⚠️ 副本仍寫 SQLCipher，以 §一第一列依據為準）。字型那一列＝ A5 的 `font-src 'self'` | ✅ **刻意不寫依賴版本號**（D6）：版本號會腐化，而這張表的功能是「看得懂的人查得動」。<br>🟢 **「三語」那一格在 v3.9.4 之前對 MCP 設定頁並不成立**（整頁繁體中文），自 [App V394-1](../appsync/App事實依據.md) 起成立。⚠️ `ja` 有 11 條 key 缺翻譯的舊債，v3.9.4 **沒有說明是否解決** —— 不得據此寫「日文已完整」 |
| 資料位置 `%APPDATA%\EYChen\Vault\vault.db`；保護三層（全庫加密／機密卡密碼／AI 取用需當場同意） | `2.how.md`「你的資料在哪裡、被誰保護」 | **App B5**（v3.7.2 已出貨，`app.setPath` 寫死）＋ [身分字串凍結清單 §B](../appsync/身分字串凍結清單.md)＋ **App A2**（全庫加密在 Free 完整無限） | ✅ ⚠️ 改名時**這一段不會跟著改**（`EYChen` 是凍結的身分字串），屆時可能需要一句說明（[webplan Q4](../webplan/關於我頁.md)） |
| 🔴 **「一般卡片的變數預設值是明文存在資料庫裡的，而且永遠會是——沒有密碼就沒有金鑰，這不是還沒做，是做不到」** | `2.how.md` 的 `notice-danger` | **誠實紅線 C2 第 1 條** ＋ **[App V38-2](../appsync/App事實依據.md)**（v3.8.0 範圍收窄至一般卡） | ✅ **全頁最重要的一格。** 官網不得用「還沒做」帶過，這是紅線原文 |
| ~~六條~~ **三條**已知邊界表（明文變數／renderer 明文抹不掉／名稱與描述不受密碼保護） | `2.how.md`「我沒有保護到的地方」 | **C2 第 1／4 條** ＋ 第 3 列出自 **[App V38-3](../appsync/App事實依據.md)** | 🙈 **另三列已隱藏**（見上方隱藏清單），理由：**作者覺得這樣講出去太死**。<br>🔴 其中「退回不加密」全站沒有第二處在講；另兩列在定價頁與 `/docs` 仍在。<br>⚠️ **要看著的一件事**：商業模式那段仍寫著「自動上鎖不加價」，而「切視窗不會鎖」的但書已經藏了。目前不踩線（那句在講價格、不在講防護），但本頁只要再多講一句自動上鎖，**C2 第 5 條就會要求那個但書回來** |
| AI 介面六條（只接受同機連線／預設不啟動／每工具一把可撤銷的通行碼／眼睛＝存在性／「有密碼」與「對 AI 隱藏」兩軸獨立／AI 傳來的參數一律忽略） | `2.how.md`「AI 是怎麼拿到東西的」 | [APP快照](../refer/APP快照.md)：v3.5.0（預設關，理由是 token 明文寫進 IDE 設定檔）、v3.6.0（兩軸正交）、v3.7.0（`inputSchema` 恆空、參數不外露） | ✅ 與 §二之四 demo 那幾列同源 |
| 🙈 **「兩句我不會說的話」整節已隱藏**：不說「PromptBox 是開源軟體」、不說「你可以自己重現我的建置」 | `2.how.md` 末節（HTML 註解） | **BRD-03 §9 規則 4**（只能說「核心開源」）＋ [BRD-02 §7.3](../refer/BRD/BRD-02_商業模式與定價.md)（open-core 啟動點在第一筆營收之後）＋ [BRD-05 §4.2](../refer/BRD/BRD-05_行銷與獲客.md)（不開兌現不了的支票） | 🙈 **2026-09-07 隱藏**，理由：**作者覺得這樣講出去太死**。<br>🔴 **後果：這兩條措辭禁令在站上已經沒有任何讀得到的地方**（§二之二 的 `words.i1`／`i2` 在孤兒頁上）。<br>📌 **但禁令本身不受影響** —— 它管的是「不准說什麼」，不是「必須說什麼」。藏起來損失的是信任素材，不是合規 |
| ~~macOS「在第一個 mac 版真的做出來之前，我不會說它可用——沒驗過的東西我不講」~~ → **2026-09-15**：「Apple Silicon 版可以下載了，但還在驗證中。需要 macOS 13（Ventura）以上、Intel Mac 不能跑；沒有經過 Apple 公證；覆蓋安裝後資料能不能完整延續我還沒實機測過——**驗完之前，我不會說它和 Windows 版一樣可靠**」 | `3.status.md`「平台支援」 | 同 §二 的 macOS 三列（build 上架、未公證、未實機驗收）；**C2** | ✅ **2026-09-15 改寫**。原句的前提（「沒有 mac 版」）在 09-14 build 上架時就失效了。「沒驗過的東西我不講」這個原則沒變，改成**明講哪些還沒驗**。<br>📌 **2026-09-27**：最低需求由 macOS 12 改為 **13（Ventura）**（隨 mac 重新上架，WL-006 §2-3）。三條揭露（未簽章、未公證、覆蓋安裝未實測）**照舊** —— App 側的 macOS 實機驗收仍未做 |
| 商業模式三句：免費版是完整軟體（邊界是**數量**不是能力）／**安全能力永不成為付費牆**／買斷 + 12 個月更新權、軟體執行時從不檢查到期日 | `3.status.md`「商業模式」 | **App A2**（能力本身不 gate）＋ [BRD-02 §3／§3.1](../refer/BRD/BRD-02_商業模式與定價.md)＋ [ADR-008](../appsync/ADR-008_授權憑證格式與離線驗證契約.md)（`exp`／`updatesUntil` 等欄位刻意不存在）＋ **App B6**（榮譽制） | ✅ 「付費版賣的不是更強的加密」與 §二之二 `words.i3` 同一條禁令 |
| ~~六條~~ **三條**不做清單（雲端同步／團隊協作／賣資料與廣告） | `3.status.md`「我不會做什麼」 | [BRD-01 §4](../refer/BRD/BRD-01_定位與市場.md) 放棄清單的**部分子集** | ✅ **不構成假主張** —— 頁面寫的是「以下幾件我不做」，沒有宣稱窮舉。<br>🙈 **另三條已隱藏**（市集抽成／版本控制與評測／硬 DRM），理由：**作者覺得這樣講出去太死**。其中硬 DRM 那條是上線當天才補進來的（原 [webplan D7](../webplan/關於我頁.md)），補完同日又藏起。<br>⚠️ 底下那句「我知道這張清單對生意不好」**仍然成立**：它舉的兩個例子（雲端、資料）都在留下的三條裡 |
| 「如果有一天我不做了」三點：軟體不會停止運作／資料是你的檔案（~~公開的標準格式（SQLCipher）……任何支援它的工具都讀得懂~~ → **開源、有公開規格的加密方案（SQLite + ChaCha20）**，2026-09-18）／我沒有你的任何東西 | `3.status.md` 末節 | **App A5**（不連網、不驗證授權）＋ [ADR-008](../appsync/ADR-008_授權憑證格式與離線驗證契約.md)（沒有到期欄位就沒有人能加那行判斷式）＋ 架構事實（無帳號、無備份、無金鑰託管） | ⚠️ **內容有依據，但整段措辭尚未經創辦人拍板**（[webplan Q1](../webplan/關於我頁.md)）。目前線上的是「照實寫得出來」的版本。<br>🔴 **2026-09-18 拿掉「任何支援它的工具都讀得懂」**（WL-006 D9）：一是 chacha20 方案的工具支援比 SQLCipher 窄；二是金鑰綁 Windows 帳戶 ＋ App 資料夾的 `Local State`，**單拿那個檔案，什麼工具都讀不了** |
| 🔴 **「目前軟體裡沒有『匯出全部資料』的功能」＋「只複製 `vault.db` 和 `keystore.bin`，在同一台電腦上也打不開」＋「複製到另一台電腦不保證解得開」** | 同上的 `notice-warning` | **C2**（已知邊界必須明列）。第三句出自 safeStorage 綁使用者帳戶（[App 側回覆 §3.1](../appsync/App側回覆.md)）；**第二句 2026-09-18 補**，出自 [App 側回覆 3 §三](../appsync/App側回覆-3.md)（不只綁帳戶，還綁 App 資料夾裡的 `Local State`） | ✅ **全站唯一講這兩件事的地方。** 它存在的理由就是「不等你哪天需要的時候才發現」，拿掉會讓前面三點被讀成更強的承諾 |
| ✅ **「從寫下第一行程式碼至今，我持續為自己遇到的問題迭代了多個版本，每一次更新都在這張清單上打勾」**（2026-09-14 改寫） | `4.origin.md` | **[changelog 歷史紀錄](../../Src/content/changelog/)**（v1.0.0 至 v3.9.4 全程迭代修復事實） | ✅ **缺口 6 於 2026-09-14 結案**：原「2026 年 2 月 11 日／27 個版本」因查無依據且每次發版即過期，已依 CLAUDE.md 紅線 2 下架該兩組數字，改為定性迭代紀錄（回寫 [PageDescription 10](./PageDescription/10_about.md) §2） |

## 二之六、會員專區與會員登入的主張（2026-09-15 新增）

> 對應 [PageDescription 11](./PageDescription/11_account.md)（`/account`）、[PageDescription 06](./PageDescription/06_privacy.md)（隱私頁的會員登入條件式段落）、[PageDescription 02](./PageDescription/02_download.md)（下載頁會員小卡）。
> 🔴 **5a+ 首次提交（2026-09-14）時本表沒有跟著回寫**，而頁面上有四句不成立的話 —— 下表第一組「已撤下」就是它們，留著是為了讓下一個人知道這幾句為什麼不能加回來。
> 🔴 **會員登入的所有主張都是條件式的**：`useMemberLogin().available` 為 false（`SUPABASE_URL` 未設定）時頁面一個字都不寫。依據欄成立的前提是「真的設定了」—— 設定前先重讀這一節。
> 📌 **2026-09-15 創辦人裁示「會員專區先藏起來」**：導覽列（桌機與行動選單）的入口同樣改為 `available` 才顯示；`/account` 本身仍可直接開啟（`nitro.prerender.routes` 明列三個語系，與 `/enterprise` 同一個作法）。

| 頁面主張 | 位置 | 依據 | 狀態 |
| --- | --- | --- | --- |
| ~~「早鳥 150 名席次保留／你的信箱已符合早鳥登記名單，享有首批永久鎖價 $19 USD/年」~~ | ~~`account.earlyBird.*`（登入即顯示）~~ | **無**。實作是 `hasEarlyBird = !!user`，沒有比對任何名單 ⇒ 對每一個登入者宣稱他在前 150 名裡。FR-27B 要求比對 Buttondown 名單；另 `$19` 寫死在語系檔（紅線 1） | ❌ **2026-09-15 撤下**。比對做好之前不顯示任何資格；做好時價格要用 `{proEarly}` 引用 `pricing.ts` |
| ~~範例授權碼 `…sample_signature_preview` ＋ 複製鈕 ＋「可直接複製並於桌面端設定頁貼上離線啟用」~~ | ~~`account.license.q12Note`~~ | **無**。金流未開通、沒有任何憑證被簽發；App 端 ADR-008 驗證也尚未實作（[SYNC-000 §三](../appsync/SYNC-000_索引與雙方進度.md)） | ❌ **2026-09-15 撤下**。等於請使用者拿假憑證去啟用 App |
| ~~「已登入會員專屬直接下載端點」~~ | ~~`account.downloads.desc`、`download.accountLead.body`「專屬下載」~~ | **無**。`/dl/*` 沒有登入閘門，檔案與公開下載頁相同 | ❌ **2026-09-15 撤下** |
| ~~「升級至 Pro」~~ | ~~`account.license.upgradeBtn`~~ | **無**。Pro 目前買不了（README §一「收不到錢」） | ❌ **2026-09-15 改為「查看 Pro 方案」** |
| 「會員登入尚未開放……下方的安裝檔與公開下載頁是同一批檔案，不需要登入」 | `account.offlineNotice.*`（未設定時） | `useMemberLogin().available` 為 false；`/dl/[platform]` 讀的是 `shared/downloads.ts`，與下載頁同一份 | ✅ |
| 「目前沒有授權碼：金流尚未開通，Pro 目前還不能購買」 | `account.license.empty`／`emptyNote` | README §一；[PRD 階段六](../webplan/PRD_階段六_金流與授權發放.md)（📦 暫緩） | ✅ ⚠️ 金流開通那天這句要換掉 |
| 會員專區「與公開下載頁是同一批官方檔案，不需要登入也能下載」 | `account.downloads.desc` | 同上 `shared/downloads.ts`；`/dl/**` 無閘門 | ✅ ⚠️ 階段六 FR-29 加上 Pro 下載閘門時要改 |
| 下載頁小卡「已經登入過？會員專區可以查看你的方案狀態，也能下載同一批安裝檔」 | `download.accountLead.*`（僅會員登入可用時顯示） | `download.vue` 的 `v-if="memberLoginAvailable"`；會員專區的方案徽章 | ✅ |
| 🔴 隱私頁 Vercel 列：「從會員專區下載安裝檔時，另記一筆下載紀錄：平台、概略國別、來源頁與 User-Agent，**不含 IP**」 | `privacy.processors.vercelData`（常駐） | `server/routes/dl/[platform].get.ts` 的 `console.info` 欄位：`platform`／`version`／`filename`／`os`／`country`／`referer`／`userAgent`／`timestamp`。**PRD FR-27C 提到的 Supabase `download_telemetry` 表尚未寫入**，本句只描述現況 | ✅ **改動遙測欄位（尤其加 IP、或開始寫進 Supabase）必須同時改隱私頁與本列**（程式檔頭已寫明） |
| 隱私頁「會員登入」段落：只有主動按「使用 Google 登入」才建立帳號；取得 Google 提供的 email、名稱、頭像網址；瀏覽／下載／訂閱都不需登入 | `privacy.collect.member.*`（條件式） | `account.vue` 的 `signInWithOAuth({ provider: 'google' })` 只綁在按鈕上；`server/database/schema.sql` 的 `profiles` 表（`email`／`display_name`／`avatar_url`）；`/dl/**` 與 `/api/subscribe` 皆無登入要求 | ⏳ 條件式，尚未顯示（Supabase 未設定）。⚠️ `schema.sql` 另有 `locale` 欄位，目前沒有任何程式寫入它；開始寫入時要補進本句 |
| 隱私頁瀏覽器儲存清單的登入 cookie（`runtimeConfig.public.supabase.cookiePrefix`），只在登入後出現、登出清除 | `privacy.storage.bodyWithCookies`／`authCookie`（條件式） | `@nuxtjs/supabase` 2.x 預設 `useSsrCookies: true`，session 存於 `sb-<專案代號>-auth-token` cookie（過長時切成 `.0`、`.1`） | ⏳ 條件式，尚未顯示 |
| 隱私頁處理者 Supabase：Google 登入提供的 email／名稱／頭像網址、登入時的 IP 與 User-Agent（驗證服務運作紀錄） | `privacy.processors.supabase*`（條件式） | `schema.sql` 的 `profiles`；Supabase Auth 的稽核紀錄含 IP | ⏳ 條件式，尚未顯示。⚠️ 開始寫入 `download_telemetry` 時，這一列要補「下載紀錄」 |
| 隱私頁處理者 Google：同意畫面上授權的 email／名稱／頭像，並知道使用者以 Google 登入本站 | `privacy.processors.google*`（條件式） | Google OAuth 標準流程（`provider: 'google'`，未額外要求 scope） | ⏳ 條件式，尚未顯示 |

## 三、商業承諾（＝規格，金流實作時逐條打勾）

> 🔴 **正本就在本 repo**：[refer/BRD/BRD-02 §3「價格表（單一真相）」](../refer/BRD/BRD-02_商業模式與定價.md)。
> 2026-08-30 創辦人確認 `Docs/refer/BRD/` 六冊即為 BRD 正本，不必再回 App repo 取（[CLAUDE.md 紅線 3](../../CLAUDE.md)）。
> 現行載體：`Src/app/utils/pricing.ts` → `Src/i18n/locales/{zh-TW,en}/pricing.json` 與 `app/pages/pricing.vue`。

| 頁面主張 | 依據 | 狀態 |
| --- | --- | --- |
| 個人版 Pro **$19 USD/年**，早鳥**永久鎖價**，**首批 150 名**（以訂閱時間戳排序） | BRD-02 §3；D25。🔴 **與 [BRD-02 §5.1 #4](../refer/BRD/BRD-02_商業模式與定價.md) 不同**：BRD 定的是「人工分批換連結」＝以**購買**計；本站以**訂閱順序**保留名額，金流開通後依序寄信確認購買（創辦人 2026-08-30 裁示，金流未開通期間只能先收名單） | ⏳ 未實作（階段 5b）。<br>✅ **2026-08-30 補上保留期**：正式開賣時依序寄信通知，**名額保留到收信後一週**（創辦人裁示），逾期釋出給下一位。文案已寫進訂閱表單 `subscribe.earlyBird.body` 與成功訊息。<br>🔴 **仍卡在 [F18](../webplan/待討論問題.md)**：授權碼記錄在會員資料，而會員專區尚未建置 |
| 含「當前版本永久可用 + **12 個月**更新權」 | BRD-02 §2；D14 兩層分離 | ⏳ |
| 永久更新權 **$149 USD** 一次買斷；發表期階梯 **$109／50 名 → $129／50 名 → $149** | [BRD-02 §3 與 §5.4](../refer/BRD/BRD-02_商業模式與定價.md)（2026-08-31 新增階梯，它同時是 §3.4 護欄 1「超過就調價或限量」的執行機制） | ⏳ **且 2026-08-31 起全站隱藏** —— 金流未開通期間顯示它只會把 $109 再錨定一次，階梯就得從已公告的數字往上爬。恢復清單見 [webplan/永久更新權上線時程.md](../webplan/永久更新權上線時程.md)。**這一列現在沒有任何頁面在用** |
| 「永久」＝永久使用 Pro 全功能 + **當前產品線**的更新與安全修補；**重大新模組另計** | [BRD-02 §3.4 護欄 2](../refer/BRD/BRD-02_商業模式與定價.md)（「現在不寫清楚，兩年後會很痛」） | ✅ **2026-08-30 寫上頁面**，取代原本的「≈ Pro 首購 + 2.6 年續訂」—— 該換算以標準價 $39 + $27×2.6 為基準，與同頁展示的早鳥價 $19 對不上（讀者按 $19 計得 $68） |
| 商業授權 **$54 USD/人/年**（2026-08-31 自 $69 調降）、**榮譽制**、**買 4 席送 1 席** | BRD-02 §3 與 **§3.1.1**（調降理由：原本的 1.77 倍是拿年費比一次性首購，用標準續訂 $27 重算為 2.56 倍、超出 §3.3 自訂區間；新價恰為 2.00 倍）；D23。榮譽制的技術理由是 **App B6**（不做硬 DRM） | ⏳ **2026-08-30 起整張卡標「還在計畫中」**（D3 → 同日再收緊）：卡片置灰，定價頁的行動點是 **disabled 的按鈕**、不再是連結 —— 在能力到位前不接受任何購買意向 |
| **持有 {sponsorSeats}（＝25）席以上的組織，可列名為支持組織** | 🔴 **BRD 沒有這一條**。創辦人 2026-08-30 新增（對標 Obsidian 的商業授權做法）<br>✅ **2026-08-31：那一頁已經存在** —— [`/enterprise`](./PageDescription/08_enterprise.md)，同日 `25` 也從三處字面值收斂進 `app/utils/pricing.ts` 的 `sponsorSeats`（紅線 1） | ⏳ 仍未實作（商業授權未開賣）。現出現於首頁商業卡、定價頁商業卡與 `/enterprise` 三處，三處都標「還在計畫中」 —— **這是它現在還不算未兌現承諾的唯一理由** |
| 🔴 **App runtime 從不檢查到期日、永不 time-bomb** | D14；[ADR-008「刻意不存在的欄位」](../appsync/ADR-008_授權憑證格式與離線驗證契約.md)（`exp`/`updatesUntil`/`maxVersion`/`machineId` 全數禁止） | ✅ **設計層面已保證** —— 欄位不存在，就沒有人能加那一行 `if` |
| 🔴 更新權到期後已安裝版本**永久正常運作，絕不鎖住或降級** | 同上；更新權執行點在官網下載閘門（D18） | ✅ 設計層面已保證 |
| ~~提供**企業發票**收據報帳~~ ⛔ **已從頁面移除** | 需要**統一編號** | ✅ **2026-08-30 解除曝險**：首頁與定價頁的商業卡不再承諾發票，改為「尚未開放購買，開放時一併公布發票與報帳方式」。[前置鏈 P2](../webplan/上線前置鏈.md) 公司登記仍未完成 —— **要重新寫回這句話，前置是 P2 完成，不是文案決定** |
| ⛔ **已移除**：商業授權「提供**專人諮詢**」 | **無依據**。[BRD-02 §3.1](../refer/BRD/BRD-02_商業模式與定價.md) 的前提是這條線「開發成本近乎為零」、功能完全同 Pro，賣的是合規與支持開發者 | ✅ 2026-08-30 移除。首頁版本本來就沒有這一句，兩頁不一致也一併收斂 |
| ⛔ **已移除**：「支援信用卡、**PayPal** 等」「授權碼**即時**寄送」 | **無依據**。金流平台尚未確定（BRD-02 內文提 Paddle、卷首註提 Polar），交付時效亦未經驗證 | ✅ 2026-08-30 改為「開放購買時一併公布」。定價頁 `faq.a6` 現在先講「尚未開放購買」，再講開放後的機制 |

---

## 🔴 目前有缺口的條目

| # | 缺口 | 對應 |
| --- | --- | --- |
| 1 | ~~**企業發票**已公開承諾，但沒有統編~~ → **2026-08-30 已從頁面撤下**（商業卡改「即將開放」）。缺口從「說出去但沒兌現」降級為「**還沒能力說**」 | [前置鏈 P2](../webplan/上線前置鏈.md)；重新寫回發票承諾前必須先完成 |
| 2 | ~~**電子報不追蹤**已寫在隱私頁，但 Buttondown 帳號尚未建立~~ → ✅ **2026-08-30 結案**：帳號已部署、開信追蹤已關 | D30 上線步驟已完成 |
| 3 | ~~**Free 版配額數字**的正本在 App repo，本 repo 只有副本~~ → ✅ **2026-08-30 結案**：創辦人確認 [refer/BRD/BRD-03](../refer/BRD/BRD-03_授權與Free-Pro邊界.md) **就是正本**，改配額直接以它為準 | [CLAUDE.md 紅線 3](../../CLAUDE.md) 已同步 |
| **5** | ~~早鳥名單收得到人，但**授權碼記錄在會員資料，而會員專區尚未建置**~~ → ✅ **2026-08-30 同日結案**：授權 email 由使用者購買時確認（預設帶入早鳥信箱），會員區做好再開賣 | [待討論問題 F18](../webplan/待討論問題.md)。⚠️ **留下一件實作待辦**：schema 要補 `user_id ↔ 授權 email` 的對應，否則會出現「憑證是 A 信箱、更新權掛在 B 帳號」的錯位（已回寫[接縫文件](../appsync/官網金流與授權發放接縫.md)） |
| 4 | **B5 護欄只釘住一半。** 2026-08-29 查核：`data-continuity.test.js` 只斷言 `appId`（`com.promptbox.app`）與 `productName`（`PromptBox`）；**userData 路徑、金鑰庫識別、DB 檔名、MCP server 識別都還沒有斷言** | 下載頁「資料原地保留」的依據強度低於它讀起來的樣子。補齊項目見 [身分字串凍結清單](../appsync/身分字串凍結清單.md)「護欄」一節與 [App 事實依據 §三](../appsync/App事實依據.md) |
| **6** | ~~🔴 **「2026 年 2 月 11 日開始寫、發了 27 個版本」在 repo 裡查不到出處**（2026-09-07 隨 `/about` 上線新增）~~ → ✅ **2026-09-14 結案**：依 [CLAUDE.md 紅線 2](../../CLAUDE.md) 下架兩個未經證實且動態過期的具體數字，改為定性迭代敘述（「從寫下第一行程式碼至今，我持續為自己遇到的問題迭代了多個版本，每一次更新都在這張清單上打勾」） | [§二之五 那一列](#二之五關於頁的主張2026-09-07-新增about-上線)；依據為 changelog 既有修復歷程。已回寫 [PageDescription 10](./PageDescription/10_about.md) §2 與 [webplan/關於我頁 Q2](../webplan/關於我頁.md) |
| **7** | ~~🔴 **更新日誌宣稱 v3.9.2，下載頁給的是 v3.7.1**（相差五個版本）~~ → ✅ **2026-09-10 當日結案**：兩顆安裝檔上 R2（`/V3.9.2/`），`href` 與 `sha256` 成對更新，兩顆的雜湊皆**整顆下載回來實算比對**過；下載頁與手冊第 9 節的校驗指令範例、三語 `download.json` 的版號同批改完 | [CLAUDE.md 紅線 5](../../CLAUDE.md) 已執行。**留下這一列是刻意的**：日誌與下載頁**是兩處分開更新的地方**，這個落差出現過一次就會出現第二次 —— 下次先動任一邊時，記得回頭看另一邊 |
| **8** | ~~🔴 **手冊第 6 章那句「機密卡無法被串聯間接讀取」沒有主詞，且與 v3.9.0／v3.9.2 的行為牴觸**~~ → ✅ **2026-09-12 結案**：主詞改為「AI」，並明寫「在你自己的預覽裡，隱藏卡同樣展不開；機密卡可就地解鎖、解鎖後可展開與複製 —— 這條隔離擋的是 AI，不是你」 | [§一 那一列](#一產品能力主張)的依據欄已填：**[App V394-3](../appsync/App事實依據.md)**，直接查 App v3.9.4 原始碼得來（不是發版說明）。回寫 [PageDescription 04](./PageDescription/04_docs.md) 第 6 章那列 |
| **9** | ~~🔴 **更新日誌宣稱 v3.9.4，下載頁給的是 v3.9.2**（R2 上查無兩版的安裝檔）~~ → ✅ **2026-09-12 當日結案**：安裝檔上 R2（`/V3.9.4/`），`href` 與 `sha256` 成對更新、創辦人實算比對；三語 `download.json` 版號、下載頁與手冊第 9 節的校驗指令範例同批改完。**第 7 條的同一個落差，第二次** | [CLAUDE.md 紅線 5](../../CLAUDE.md) 已執行。⚠️ **這次第一輪只改了 zh-TW**：`en`／`ja` 的 `download.json` 與手冊第 9 節是提交前才補上的 —— 下次結案時照上面那串逐項對 |
| **10** | ~~🔴 **官網仍把「對 AI 隱藏」叫成「鎖」，App 自 v3.9.4 起不再這樣叫**~~ → ✅ **2026-09-12 同日結案**：首頁 `home.advanced.row1.b3`（三語）改用 App 按鈕原文、手冊第 5 章 navLabel 改為「機密卡片與 AI 可見性」、手冊第 6 章「鎖定卡」隨第 8 條整句改寫 | [§一「一鍵『對 AI 隱藏』」那列](#一產品能力主張)；[App V394-2](../appsync/App事實依據.md)。已回寫 [PageDescription 01](./PageDescription/01_index.md) §2 與 [PageDescription 04](./PageDescription/04_docs.md)。📌 **v3.0.0 條目的「資料鎖」是歷史名稱，不改** |
| **11** | ~~🔴 **本 repo 的 BRD 正本落後 App v3.9.3**~~ → ✅ **2026-09-12 同日結案**：App repo BRD-999 A-9、A-8 依序貼進 [BRD-03 §4.3、§4.0-b／§4.0-c](../refer/BRD/BRD-03_授權與Free-Pro邊界.md)（原論證保留供對照）；[BRD-00](../refer/BRD/BRD-00_索引與決策狀態.md) 決策 #26 改寫、新增 #28、§7.3 補一筆 | [App V393-1／V393-2](../appsync/App事實依據.md)。**App repo 那側的 BRD-999 兩則已改 ✅ 已同步**（該 repo 另行提交）。📌 [CLAUDE.md 紅線 3](../../CLAUDE.md)：正本在這裡，App 側的「待同步」清單要靠這一側把它清掉 |

第 2、3、5 條已於 2026-08-30 結案，第 1 條的曝險已由「把話撤下」解除（能力到位前不再寫回去）；第 7 條於 2026-09-10 結案；第 8、9、10、11 條於 2026-09-12 結案；第 6 條於 2026-09-14 結案。

**現在真正懸著的缺口僅剩第 4 條。**

第 4 條是**說出去的話只有一半有機器在守** —— 不會今天出事，會在改名那天出事。
第 6 條已於 **2026-09-14 結案**：已下架「2026 年 2 月 11 日／27 個版本」兩組動態過期且無依據之數字，改為定性迭代紀錄。
第 7 條已於 **2026-09-10 當日結案**：檔案上了 R2、`href` 與 `sha256` 成對更新、雜湊整顆實算比對過。
第 8 條已於 **2026-09-12 結案**：已加回「AI」主詞並明寫隔離範圍。
第 9 條是第 7 條的重演（2026-09-12），同日結案：日誌先走了兩版，下載頁當天跟上。
第 10、11 條是同一天補 v3.9.3／v3.9.4 時查出來的「App 改了、官網這一側沒跟」，已於 **2026-09-12 雙雙結案**：第 10 條詞彙改為「對 AI 隱藏」，第 11 條 BRD 正本回寫。

> 🔴 **而改名已經是進行式**：2026-08-30 確認**目前沒有既有使用者**，
> [身分字串凍結清單](../appsync/身分字串凍結清單.md) 因此走 **§B 重選分支** ——
> 那是**最後一次**可以自由決定身分字串的機會。第 4 條的護欄要跟著新選的字串一起補，
> 不是補現在這組。
