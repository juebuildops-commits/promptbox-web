# 主張與依據（Claims & Evidence）

> **用途**：官網上每一句**對外主張**，配一條**可查證的依據**與**目前狀態**。
> **建立**：2026-08-28　·　**最後查核**：2026-08-30
> **為什麼需要它**：F11（「開源發布於 GitHub Releases」）、「也提供 macOS 版」、
> 「校驗碼供您核對**來源**」、「提供企業發票」—— 這四個問題全部是**事後一個一個撞出來的**，
> 而且是同一種失效模式：**頁面上有一句話，而它的依據不成立或已過期**。這張表把那件事變成可檢查的。
>
> **上位原則**：誠實紅線 C1 / C2 —— 誠實是紅線，不是美德。
> 「不得在任何介面或文案暗示超出實際的保護」「已知邊界必須明列，不得模糊」。
> 全文見 [App 事實依據 §二](../appsync/App事實依據.md)。

---

## 怎麼用

- **改文案之前**：先看這裡有沒有對應的列。沒有 ⇒ 你正在新增一條沒有依據的主張。
- **依據欄的三種寫法**，不可混用：
  - **連結** —— 依據就在這個 repo，點得開
  - **`App A5` 這種編號** —— 依據是 App 的行為，正本在 App repo，本 repo 只有 [App 事實依據](../appsync/App事實依據.md) 的快照
  - **「無」** —— **真的沒有依據**。不要為了讓表格好看而編一個連結出來
- **不必一次填完**。[PageDescription](./PageDescription/) 的「核心內容映射」是主張的來源清單，邊改邊補即可。

---

## 一、產品能力主張

> 本節的依據全部在 App 端。編號對應 [App 事實依據 §一](../appsync/App事實依據.md)，
> 🔴 **正本在 App repo，本 repo 沒有 ADR 副本**——需要完整脈絡時請自行去 App repo 取。

| 頁面主張 | 位置 | 依據 | 狀態 |
| --- | --- | --- | --- |
| 100% 完全不連網（Zero Outbound） | 首頁 `faq.a1`、下載頁 `notes.n1`、隱私頁 | **App A5**。護欄測試 `electron/__tests__/no-outbound.test.js`（7 條）擋回加 | ✅ |
| 唯一的網路元件是內建 MCP 伺服器，**只監聽 `127.0.0.1`**、不對外連線 | 首頁 `faq.a1`、下載頁 `notes.n1`、隱私頁 `appVsSite`、[手冊第 7 章](../../Src/content/docs/7.mcp.md) | **BRD-03 §9 規則 6**：講「不連網」時若不一併說明本機 listener，會被技術使用者抓語病 —— **主動說反而加分** | ✅ 2026-08-30 補上前三處（原本只有手冊有，而主張講在另外三頁） |
| SQLCipher 頁級靜態加密，金鑰由 DPAPI / Keychain 保護 | 下載頁 `notes.n2`、首頁 `faq.a1` | **App A1**（安全是產品本體） | ✅ |
| 機密卡片 scrypt + AES-256-GCM，AI 取用須人類授權才釋放 | 首頁 `faq.a2`、[手冊第 5 章](../../Src/content/docs/5.secrets.md) | **App A3 / A4**（機密預設不自動吐出、密鑰威脅模型） | ✅ |
| 授權時可勾「10 分鐘內不再問我」，該期間不重複詢問（純記憶體、不快取密碼、鎖屏即失效） | 首頁 `faq.a2`、手冊第 5 章 | v3.6.0 行為，[changelog v3.6.0](../../Src/content/changelog/v3.6.0.md) | ✅ 2026-08-30 補進首頁（原本 FAQ 只寫「單次釋放」，讀起來像每次都要重新授權） |
| 🔴 **卡片名稱、說明、變數名稱與變數預設值「不」受密碼保護** | 首頁 `faq.a2`、手冊第 5 章邊界告示 | **BRD-03 §9 規則 1／2／7**、ADR-006（機密卡變數預設值以明文儲存）。規則 7 能講的是「內容需要你按下允許才會交給 AI，一次都不能少」，不能講「設了密碼這張卡的一切都藏起來了」 | ✅ **2026-08-30 首次揭露**。此前首頁寫「參數名與內容強制不可見」、changelog v3.7.0 寫「連參數名都不對外揭露」，**兩處都是規則 7 明令不得出現的措辭**，已一併改掉 |
| 升級覆蓋安裝，安裝路徑不變、資料原地保留 | 下載頁 `notes.n1`、首頁 `faq.a3`（2026-08-31 起不再包含 `notes.n3`／v2.x 升級敘述） | **App B5** 資料連續性紅線；[身分字串凍結清單](../appsync/身分字串凍結清單.md) | ⚠️ 原則成立，但**護欄只覆蓋一半**——見 §缺口 4 |
| 核心功能永久免費、配額只擋新增不刪既有 | 首頁 `faq.a5`、定價頁 | **App A2**（安全能力永不成為付費牆）、[FP04 §四](../refer/FP04.md) | ⚠️ 具體配額數字見 §缺口 3 |
| 🔴 **45 是「卡片總數」，機密卡與串聯卡都算在內**（不是 45+3+5=53） | 首頁 `price.free.f2`／`faq.a5`、定價頁 Free 卡與對照表 r2–r4 | [BRD-03 §2.0](../refer/BRD/BRD-03_授權與Free-Pro邊界.md) 總數口徑定案；§4.0-b2 在 App 端明令標籤不得寫成會被相加的形式 | ✅ **2026-08-30 改為總數口徑**。此前四處把三個數字平行並列，正是 §4.0-b2 要避免的誤讀 |
| 支援 Windows 10/11、MCP 接 Cursor / VS Code / Claude Desktop 等 | 首頁 `faq.a4`、下載頁 | [App README 快照](../refer/APP快照.md)（v3.7.1） | ✅ |
| 🔴 **Free 完整擁有：全庫靜態加密（永久免費、不設限）、變數樣板與變數替換、機密釋放授權窗與閒置自動鎖、釋放稽核的紀錄與檢視** | 首頁與定價頁的 Free 卡、定價頁對照表 r7／r8／r13 | [BRD-03 §1 G1](../refer/BRD/BRD-03_授權與Free-Pro邊界.md)（加密永久免費，對外口徑必須是「加密永遠免費；Pro 賣的是規模與釋放護欄」）、§2 真相表、§2.2（稽核記錄與檢視不 gate）、§2.5（授權窗與閒置自動鎖不 gate）、§2.6（變數替換不 gate） | ✅ **2026-08-30 首次上頁**。此前定價頁的免費卡從頭到尾沒有「加密」兩個字，等於 G1 規定的對外口徑一次都沒被說出來 |
| 🔴 **Pro 相對 Free 只多兩件事：解除四項配額 + 釋放稽核匯出** | 首頁與定價頁的 Pro 卡、對照表 r14 | **BRD-03 §9 規則 3** 的正確口徑；§2.6 ② 強型別自 v3.7.0 **全面停用**，「停用期間不得再提強型別，升級頁與行銷文案一併移除」 | ⚠️ **稽核匯出尚未實作**（§2.2「唯一沒有的是匯出」、§7 P3 列為 Pro 上線工作項），頁面標示為「隨 Pro 版一同推出」。Pro 整體本就未上架（Build B 未做，[BRD-04 §5](../refer/BRD/BRD-04_版本路線與時序.md)），四張卡的行動點都是登記表單 |
| ⛔ **已移除**：「動態變數樣板」與「資料匯出／匯入（JSON／CSV）」曾被列為 Pro 專屬 | 原 `pricing.pro.f5`／`f6`、`home.price.pro.f4`／`f5`、對照表 r7／r8 | **無依據**。變數在 BRD-03 §2 是「無限｜不 gate」，§2.6 明文「gate 它等於 gate 變數」；資料匯出／匯入**不在真相表上**。若照原文案實作＝[BRD-04 §5.1](../refer/BRD/BRD-04_版本路線與時序.md) 最忌的 clawback（收回已免費的功能） | ✅ 2026-08-30 移除（D1 決策 a）。**留下這一列是刻意的** —— 下一個人看到 Pro 賣點變薄時，要能一次讀到它為什麼變薄 |

## 二、發布與信任主張

| 頁面主張 | 位置 | 依據 | 狀態 |
| --- | --- | --- | --- |
| 安裝檔由官網直接發布 | 下載頁 `hero.lead`、`trust.t1` | `download.vue` 的 R2 常數；[上線前置鏈 P1](../webplan/上線前置鏈.md) | ✅ 2026-08-28 起由 Cloudflare R2 發布 |
| 附 SHA-256 校驗碼，可核對**檔案完整性** | 下載頁 `checksum.*`、首頁 `faq.a3` | `download.vue` 的 `sha256` 常數。**已用 R2 ETag（單段上傳＝物件 MD5）驗證線上檔案與本機產物位元組相同**（2026-08-28） | ✅ |
| 🔴 **尚未購買程式碼簽章憑證**，Windows 會顯示「無法辨識的應用程式」 | 下載頁 `notes.n4`、[手冊第 9 節](../../Src/content/docs/9.install-safety.md) | [上線前置鏈 **P5**](../webplan/上線前置鏈.md) | ✅ **已誠實揭露**（依誠實紅線 C2「已知邊界必須明列」） |
| macOS 10.15+ 原生支援、Apple Silicon 與 Intel 雙架構 | 下載頁 mac 卡片；**首頁 CTA ×4（2026-08-30 補正）**；**首頁 `trust.platforms`／`faq.a4`、下載頁 `meta.title`／`meta.description`（2026-08-30 第二次補正）** | **無 build**。依賴 [P3 D-U-N-S → P4 Apple Developer](../webplan/上線前置鏈.md) | ⚠️ 已改為「即將推出」+ 導向訂閱，不給假的下載鈕 |
| 本站未載入任何分析工具 | 隱私頁 | `NUXT_PUBLIC_GTAG_ID` 留空 ⇒ 完全不注入 gtag（D4） | ✅ |
| 電子報**不使用開信追蹤像素** | 隱私頁；**訂閱表單 `subscribe.privacyNote`（現為 5 頁：首頁／download／pricing／changelog／privacy）** | D29 選 Buttondown 的理由；需在 Buttondown 後台**實際關閉**（[D30 上線步驟 2](../webplan/待討論問題.md)） | ✅ **2026-08-30 已兌現**：帳號已建立、開信追蹤已於後台關閉（創辦人確認） |
| 訂閱信箱**不轉售、不共用，可隨時一鍵退訂** | 訂閱表單 `subscribe.privacyNote`（5 頁） | 無第三方接收者：`server/api/subscribe.post.ts` 只呼叫 Buttondown 一個端點；退訂由 Buttondown 提供 | ✅ 2026-08-30 起 Buttondown 已部署 |
| 送給 Buttondown 的個資**只有使用者主動提供的 email**（不含 IP） | 隱私頁第三方處理者矩陣；`subscribe.privacyNote` | 🔴 **路線 B**（2026-08-30 裁示）：`subscribe.post.ts` 刻意**不轉發訪客 IP**，即使 Buttondown 官方建議這樣做可避免防火牆誤判。代價由我方限流承擔 | ✅ 已實作。**改動此決定必須同時改隱私頁與本表**（程式檔頭已寫明） |
| 訂閱表單的**失敗訊息陳述的是真實原因**（`subscribe.state.*`） | 5 頁訂閱表單 | `subscribe.post.ts` 的分支與 HTTP 狀態碼一一對應；`blocked` 分支現有 `console.error` 記錄 `firewall_reasons` | ⚠️ **2026-08-30 修正過一次**。`blocked` 原本斷言「含 `+` 的別名信箱常遇到」＋「重送同一個不會有不同結果」——**兩句都是從一次觀察推出的，第一句已被實測推翻**（見 [F17](../webplan/待討論問題.md)）。現行文案**不對原因、也不對可重試性表態** |
| App 自 v3.3.0 起完全不連網，**永不主動檢查更新**，故 email 是唯一的新版通知管道 | 訂閱表單 `subscribe.updates.body`（首頁 2026-08-30 起也顯示此句）；D20 | **App A5** 零外連（同 §一 第 1 列）；[APP快照](../refer/APP快照.md) 的 v3.3.0 版本歷程 | ✅ |

> ✅ **2026-08-30 結案**：Buttondown 已成功部署、開信追蹤已在後台關閉（創辦人確認）。
> 上面三列的共同前提（`NUXT_BUTTONDOWN_API_KEY` 未設定 ⇒ 端點回 501）已經解除，
> 五頁的訂閱表單現在是真的會收人的。
>
> ✅ **F18 已於同日結案（2026-08-30 創辦人裁示）**：授權 email 由使用者**購買時確認，
> 預設帶入早鳥登記的信箱**。因此訂閱表單「這個信箱也是日後授權憑證上簽的那一個」
> **成立** —— 前提是購買流程要以它為預設值，這一條已寫進
> [待討論問題 F18](../webplan/待討論問題.md) 與
> [接縫文件](../appsync/官網金流與授權發放接縫.md)。
> 憑證綁 email 本來就是 [ADR-008](../appsync/ADR-008_授權憑證格式與離線驗證契約.md) / D12 早已定的事。

> 🔴 **2026-08-30 補正：這條裁決原本只套用在下載頁，首頁漏了。**
>
> 首頁四顆 CTA 有兩顆寫著「免費下載 macOS 版」（`common.cta.downloadMac`），
> 而 macOS build 不存在 —— 依 §上位原則的誠實紅線 C1，那是**暗示了超出實際的結果**。
> 更糟的是它還是視覺主鈕（實心品牌色），能用的 Windows 反而是次要鈕。
>
> 已改為 `common.cta.macSoon`（「macOS 版即將推出」／"macOS coming soon"），
> 主鈕樣式讓給 Windows。舊 key `downloadMac` 已從三個語系移除（全站只有首頁在用）。
>
> **為什麼會漏**：Q15-b 清點的是「GitHub Releases」相關字串，
> 首頁那兩顆的問題是**承諾一個不存在的平台**，不在那次清點的關鍵字裡。
>
> 🔴 **同日第二次補正：CTA 改完之後，還有三處在用文字宣稱已支援。**
> 首頁 `trust.platforms`（「Windows ・ macOS」）、首頁 `faq.a4`（「以及 macOS 10.15 Catalina 以上，原生支援 Apple Silicon 與 Intel」）、
> 下載頁 `meta.title`／`meta.description`（「Windows / macOS」「支援 … macOS 10.15+」）。
> 同一個缺位的 build，站上一度有四種說法，其中 meta 還是搜尋結果直接顯示的那一句。
> **教訓與上一次相同**：按鈕改完不等於主張改完 —— 一個平台名稱會散在 CTA、信任列、FAQ 與 meta 四種載體。

## 二之二、「我們不做什麼」頁的主張（2026-08-30 新增，D5）

> 這一節整體對應 [PageDescription 07](./PageDescription/07_what-we-dont-do.md)。
> 🔴 **這一頁的每一列都沒有機器在守**，而每一條禁令都對應一句「說了會更好賣」的文案 —— 它是全站最容易被日後悄悄侵蝕的一頁。
>
> 🔴 **2026-09-03 導覽下架（不是主張下架）**：創辦人認為「我們不做什麼」語氣過死，該項已自頁尾導覽移除，本頁成為**站內無入口的孤兒頁**。
> **下表每一列仍然有效且仍在線上** —— 網址還開得起來，內容一個字沒改，因此 C1／C2 誠實紅線照舊適用，改動時仍要回寫本節。
> 變的只有一件事：這些自我約束**不再有人看得到**，而它們原本的職能是降低背書成本（見 [BRD-05 §5](../refer/BRD/BRD-05_行銷與獲客.md)「放棄清單本身就是行銷素材」）。
> ⚠️ 這一項在 [BRD-00 §決策狀態](../refer/BRD/BRD-00_索引與決策狀態.md) 與 [BRD-05 §8](../refer/BRD/BRD-05_行銷與獲客.md) 都還是未打勾的首發交付項，**本次口頭決策尚未回寫 BRD 正本**。

| 頁面主張 | 位置 | 依據 | 狀態 |
| --- | --- | --- | --- |
| 六條主動放棄清單（團隊協作／雲端同步當賣點／版本控制與評測／prompt 市集／廣告與資料變現／硬 DRM） | `features.i1`–`i6` | [BRD-01 §4](../refer/BRD/BRD-01_定位與市場.md) 的放棄清單，逐條對應、不增不減；[BRD-05 §5](../refer/BRD/BRD-05_行銷與獲客.md)「首發即上」 | ✅ |
| 🔴 **不說「PromptBox 是開源軟體」——「現在還沒有公開的 repo」** | `words.i1` | **BRD-03 §9 規則 4**（只能說「核心開源」）＋ [BRD-02 §7.3](../refer/BRD/BRD-02_商業模式與定價.md)（open-core 啟動點在第一筆營收之後）＋ [BRD-00 U9](../refer/BRD/BRD-00_索引與決策狀態.md)（界線衝突未結案） | ✅ **這是目前唯一誠實的寫法**。<br>⚠️ repo 真的公開的那一天，要同時改：這一格的文案、本列、PageDescription 07 的 §2 與 §3（會多一條外連） |
| **不承諾可重現建置**；校驗碼只證明「檔案與我們發布的一致」 | `words.i2` | [BRD-05 §4.2](../refer/BRD/BRD-05_行銷與獲客.md)「不要開出兌現不了的支票」；標準 binary 含閉源模組 | ✅ 與 §二「附 SHA-256 校驗碼」列互為表裡 —— 一列講校驗碼證明什麼，一列講它不證明什麼 |
| 不暗示 Pro 提供更強的加密；變數預設值在 Free 與 Pro 都是明文 | `words.i3` | **BRD-03 §9 規則 1／2**、§1 G1、ADR-006 | ✅ 與 §一「卡片名稱／變數預設值不受密碼保護」列同源 |
| 不暗示機密卡「設了密碼一切都藏起來」 | `words.i4` | **BRD-03 §9 規則 7** | ✅ |
| 不單獨講「完全不連網」，必定同時說明 MCP 本機 listener | `words.i5` | **BRD-03 §9 規則 5／6** | ✅ 全站四處講「不連網」的地方現在都成對出現 |

## 二之三、「支持組織」頁的主張（2026-08-31 新增）

> 這一節整體對應 [PageDescription 08](./PageDescription/08_enterprise.md)。
> 這一頁是 §三「25 席可列名」那一列所掛的產品待辦的兌現物。
> 🔴 **它承載的未來承諾密度是全站最高的，而其中沒有一項今天可以兌現** ——
> 也因此它與 §二之二 一樣：**每一列都沒有機器在守**。

| 頁面主張 | 位置 | 依據 | 狀態 |
| --- | --- | --- | --- |
| 🔴 **「這面牆現在是空的，還沒有任何一家組織」** | `hero.*`、`wall.emptyTitle`／`emptyBody` | 事實：商業授權尚未開放購買（D3，定價頁商業卡的行動點是 disabled 的 `<button>`）。**這是本頁的主體內容，不是待填的坑** | ✅ |
| 這面牆不會出現：①沒有實際付費的公司標誌（含使用中／測試中／洽談中）②案例研究與推薦語 ③「受各大企業信賴」這類查不到數字的集合名詞 | `wall.never.i1`–`i3` | 誠實紅線 **C1**（不得暗示超出實際）在本頁的具體形態。三條都是**縮小自身主張**的自我約束，性質同 §二之二 的措辭禁令 | ✅ ⚠️ 第一次有公司表達興趣時，`i1` 會立刻變成一句擋路的話 —— 這是本頁最可能被悄悄拿掉的一列 |
| 列名的**唯一**條件是 25 席；沒有另外的贊助方案、沒有付費置頂、不分等級 | `wall.rule` | 創辦人 2026-08-30 裁示能撐住的只有「25 席以上可列名」一句。<br>🔴 對標對象 Obsidian 用六層寶石分級，**本站刻意不採用** —— 分級會是一個沒有依據的新承諾 | ✅ |
| 商業授權 ${commercial}／人／年，**純年費、不提供買斷** | `license.i2` | [BRD-02 §3 價格表](../refer/BRD/BRD-02_商業模式與定價.md)、§3.1（買斷等於告訴公司「付一次就永久合規」，會消滅第二年續購動機） | ⏳ 未開賣。與定價頁同一組數字，皆出自 `app/utils/pricing.ts` |
| 功能與個人版 Pro **完全相同**，商業授權不解鎖任何額外功能 | `license.i1`、`license.lead` | [BRD-02 §3.1](../refer/BRD/BRD-02_商業模式與定價.md)：它賣的是合規與支持開發，不是功能 | ⏳ |
| 買 4 席送 1 席（${commercialBundleTotal}／年 發 {commercialBundleSeats} 把獨立 key） | `license.i3` | [BRD-02 §3.2](../refer/BRD/BRD-02_商業模式與定價.md) 促銷表「✅ 首發做」 | ⏳ |
| 榮譽制：App 端不檢查到期、無機器碼綁定、無密碼狗；**到期後已安裝版本永久正常運作，不鎖不降級** | `license.i4`、`trust.i4` | [BRD-02 §3.1](../refer/BRD/BRD-02_商業模式與定價.md)＋[ADR-008「刻意不存在的欄位」](../appsync/ADR-008_授權憑證格式與離線驗證契約.md)（`exp`／`updatesUntil`／`maxVersion`／`machineId` 全數禁止） | ✅ 設計層面已保證（欄位不存在＝沒有人能加那一行判斷式） |
| **尚未開放購買**；不接受任何購買意向；發票與報帳方式待開放時一併公布 | `license.notOpenTitle`／`notOpenBody`、`meta.description` | D3 同一裁決（與定價頁商業卡**必須同進退**）；發票需統一編號，[前置鏈 P2](../webplan/上線前置鏈.md) 未完成 | ✅ 與 §缺口 1 同源 |
| 零主動外連，**且同句說明內建 MCP 只監聽 `127.0.0.1`** | `trust.i1` | **App A5** ＋ **BRD-03 §9 規則 5／6**。§一第 1／2 列的複述，**不是新主張** | ✅ 🔴 拆成兩條會讓第一條變成不完整的主張 |
| SQLCipher + DPAPI／Keychain、機密卡 scrypt + AES-256-GCM 需人類授權；**同段揭露卡片名稱／說明／變數名稱／變數預設值不受密碼保護** | `trust.i2` | **App A1／A3／A4** ＋ **BRD-03 §9 規則 1／2／7**。§一第 3／4／6 列的複述 | ✅ |
| 無伺服器、無帳號、無租戶，**因此備份與裝置遺失的風險由客戶自己的 IT 流程承擔** | `trust.i3` | 架構事實（全站唯一 server route 是 `/api/subscribe`）；後半句是 C2「已知邊界必須明列」——**主動講出代價，不只講好處** | ✅ |
| 「開放購買時我們會主動聯絡你」 | `cta.body`、`license.notOpenBody` | 與 `subscribe.commercial.body` 既有的同一句承諾（定價頁與隱私頁已在用），管道是同一份 Buttondown 名單 | ⏳ 依賴商業授權真的開賣 |
| ⏳ **列名同意怎麼取得 —— 沒有裁示** | 頁面上**刻意沒有任何一句**描述列名流程 | **無**。使用公司名稱與商標需要對方授權，而「買了授權就自動被公開列名」對某些客戶是採購阻礙而非好處 | 🔴 **放上第一個標誌之前必須先有答案**，與發票、報帳同一批決定。在有裁示前不得在頁面上加描述流程的句子（見 [PageDescription 08 §4](./PageDescription/08_enterprise.md)） |

## 二之四、「模擬試用」頁的主張（2026-09-05 新增）

> 這一節整體對應 [PageDescription 09](./PageDescription/09_demo.md)。
> 🔴 **這一節存在的唯一理由，是 `public/demo-app/index.html` 被刻意凍結。**
> 那份 1857 行的示範裡有三處與 App 實情不符（見下表第一列）。創辦人 2026-09-05 決定**不修它**，
> 改由外殼那層標明「這是示意」。**因此第一列不是補充說明，是其餘一切成立的前提** ——
> 拿掉那句聲明，就必須回頭修 demo 本體，兩者只能二擇一。

| 頁面主張 | 位置 | 依據 | 狀態 |
| --- | --- | --- | --- |
| 🔴 **「畫面為互動示意，工具名稱、連接埠與連線細節經過簡化，與實際的 MCP 介面不同」** | `demo.disclaimer.title`／`body`（`/demo` 頁的 `notice-warning`）、首頁 `home.demo.note`（英文版另註介面語言） | 誠實紅線 **C1**。demo 內已知三處落差，依據 [APP快照](../refer/APP快照.md)：①工具名寫 `list_prompts()`／`get_prompt({id})`，實為 MCP 標準 `tools/list`／`tools/call`，每張未鎖定卡暴露為 `prompt_{id}`（v3.0.0）②寫死 `127.0.0.1:8765`，實際 port 由 `electron/portDetector.cjs` **動態偵測**（`/sse` 端點本身為真）③工具回傳區印 `variables: …`，但機密卡 `inputSchema` **恆空、連參數名都不列**（v3.7.0），且「變數替換」設定**預設關閉**、只管一般卡 | ✅ 🔴 **這句話是 demo 內容凍結的交換條件**（[webplan/模擬試用頁.md](../webplan/模擬試用頁.md) D7）。要刪它 ⇒ 先修 demo |
| 「此示範不做真加密，你輸入的密碼只存在這個分頁的記憶體裡」 | `demo.disclaimer.body`；demo 內部亦有兩處自述 | 事實：`demo-app/index.html` 的密碼以明文存在 JS 狀態並直接字串比對。C2「已知邊界必須明列」 | ✅ |
| 「全部在瀏覽器裡跑，不會送出任何東西」 | `demo.hero.lead` | 事實：demo 為零外部依賴的單檔 HTML，無 fetch／XHR／WebSocket；外殼頁亦不對它發任何請求 | ✅ |
| 眼睛＝存在性、鎖頭＝密碼是**兩條正交的軸**；「機密且隱藏」是合法狀態 | demo 互動本身（卡片可同時顯示兩個徽章） | [APP快照](../refer/APP快照.md) v3.6.0「全站不再一符二義」 | ✅ 與 §一第 6 列同源 |
| 機密卡**仍會出現在** AI 的清單裡（標記為需授權），內容才需要 consent | demo 的 `list_prompts` 輸出 | v3.6.0「`tools/list` 放寬存在性，`is_secret` 退出過濾條件；對 AI 可見的僅限名稱、描述、ID」 | ✅ |
| 對 AI 隱藏之後，**AI 端沒有任何線索知道那張卡存在** | demo 清單下方的說明行 | v3.6.0：可見性全交給 `is_locked`，關掉眼睛的卡不進 `tools/list` | ✅ 與 §一「關掉眼睛後 AI 連這張卡存在都不知道」同源 |
| 授權窗「10 分鐘內不再問我」：綁單卡與單一來源、鎖屏／睡眠／手動撤銷立即失效、**窗內每次取用仍會記錄** | demo 的 consent 彈窗與卡片上的倒數徽章 | v3.6.0 逐字（純牆鐘、不跨 token、絕不落盤；稽核以 `result='allow_window'` 區分）；[BRD-03 §1](../refer/BRD/BRD-03_授權與Free-Pro邊界.md) 第 42／90 行（授權窗與稽核記錄皆不 gate、Free 也有） | ✅ 與 §一第 5 列同源 |
| 「密碼從頭到尾沒有離開過你的機器」 | demo 授權成功後的 AI 回覆 | v3.6.0「只快取明文，不快取密碼或派生金鑰 —— 『密碼永不快取』這條不變式沒有破」 | ✅ |
| **未填的變數原樣保留 `{{name}}` 佔位符**，不會靜默替換成空字串 | demo 的複製與工具回傳兩條路徑 | [APP快照](../refer/APP快照.md) v3.7.0：該版特意修掉「替換成空字串會讓輸出安靜地少一段」 | ✅ |
| MCP 伺服器**預設不啟動**，要由使用者在 App 這一側按下開關 | demo 的初始狀態與 MCP 狀態條 | v3.5.0「MCP 不再無條件自動啟動（預設關）：token 是明文寫進 IDE 設定檔的，伺服器永遠在跑等於暴露窗永遠開著」 | ✅ 🔴 demo 把開關放在 App 側而非 IDE 側是同一個理由 —— 放 IDE 會演成「IDE 一鍵就連上」，那是假的 |
| 不連 MCP 也能用：複製貼上那條路徑**永遠可用** | demo 右側的貼上欄（未連線時仍可送出） | 事實（GUI 複製不經 MCP）；亦是 [BRD-03 §5](../refer/BRD/BRD-03_授權與Free-Pro邊界.md) 第 1 號風險「MCP 設定門檻會讓付費牆變成沒人走到的門」的直接回應 | ✅ |
| ⏳ **demo 未演示 Free 配額**（卡片總數 45／機密卡 3／串聯 5／Token 2） | demo 內沒有任何配額提示 | 不構成錯誤主張（沒說「無限」），但 [BRD-03 §2.1](../refer/BRD/BRD-03_授權與Free-Pro邊界.md) 指出「第 4 張機密卡的升級動機遠強於第 46 張」，而 demo 剛好有 4 張卡 | ⏳ **機會而非缺陷**：在「把第 4 張設為機密」時提示 Free 只有 3 張，可把示範接上 `/pricing`。要做 ⇒ 得先解除 D7 的內容凍結 |
| ⚠️ **英文外殼包中文 demo** | `/en/demo` 全頁；入口在 `/en` 首頁那一段 | 事實：demo 內文未 i18n（[webplan/模擬試用頁.md](../webplan/模擬試用頁.md) D5／D10）。英文 `disclaimer.body` 與 `home.demo.note` **各有一句明說介面為繁體中文** | ✅ 🔴 那兩句是這件事不構成誤導的**唯一**依據，翻譯時不得省略 |

## 三、商業承諾（＝規格，金流實作時逐條打勾）

> 🔴 **正本就在本 repo**：[refer/BRD/BRD-02 §3「價格表（單一真相）」](../refer/BRD/BRD-02_商業模式與定價.md)。
> 2026-08-30 創辦人確認 `Docs/refer/BRD/` 六冊即為 BRD 正本，不必再回 App repo 取（[CLAUDE.md 紅線 3](../../CLAUDE.md)）。
> 現行載體：`Src/app/utils/pricing.ts` → `Src/i18n/locales/{zh-TW,en}/pricing.json` 與 `app/pages/pricing.vue`。

| 頁面主張 | 依據 | 狀態 |
| --- | --- | --- |
| 個人版 Pro **$19 USD/年**，早鳥**永久鎖價**，**首批 150 名**（以訂閱時間戳排序） | BRD-02 §3；D25。🔴 **與 [BRD-02 §5.1 #4](../refer/BRD/BRD-02_商業模式與定價.md) 不同**：BRD 定的是「人工分批換連結」＝以**購買**計；本站以**訂閱順序**保留名額，金流開通後依序寄信確認購買（創辦人 2026-08-30 裁示，金流未開通期間只能先收名單） | ⏳ 未實作（階段 5b）。<br>✅ **2026-08-30 補上保留期**：正式開賣時依序寄信通知，**名額保留到收信後一週**（創辦人裁示），逾期釋出給下一位。文案已寫進訂閱表單 `subscribe.earlyBird.body` 與成功訊息。<br>🔴 **仍卡在 [F18](../webplan/待討論問題.md)**：授權碼記錄在會員資料，而會員專區尚未建置 |
| 含「當前版本永久可用 + **12 個月**更新權」 | BRD-02 §2；D14 兩層分離 | ⏳ |
| 永久更新權 **$149 USD** 一次買斷；發表期階梯 **$109／50 名 → $129／50 名 → $149** | [BRD-02 §3 與 §5.4](../refer/BRD/BRD-02_商業模式與定價.md)（2026-08-31 新增階梯，它同時是 §3.4 護欄 1「超過就調價或限量」的執行機制） | ⏳ **且 2026-08-31 起全站隱藏** —— 金流未開通期間顯示它只會把 $109 再錨定一次，階梯就得從已公告的數字往上爬。恢復清單見 [webplan/永久更新權上線時程.md](../webplan/永久更新權上線時程.md)。**這一列現在沒有任何頁面在用** |
| 「永久」＝永久使用 Pro 全功能 + **當前產品線**的更新與安全修補；**重大新模組另計** | [BRD-02 §3.4 護欄 2](../refer/BRD/BRD-02_商業模式與定價.md)（「現在不寫清楚，兩年後會很痛」） | ✅ **2026-08-30 寫上頁面**，取代原本的「≈ Pro 首購 + 2.6 年續訂」—— 該換算以標準價 $39 + $27×2.6 為基準，與同頁展示的早鳥價 $19 對不上（讀者按 $19 計得 $68） |
| 商業授權 **$54 USD/人/年**（2026-08-31 自 $69 調降）、**榮譽制**、**買 4 席送 1 席** | BRD-02 §3 與 **§3.1.1**（調降理由：原本的 1.77 倍是拿年費比一次性首購，用標準續訂 $27 重算為 2.56 倍、超出 §3.3 自訂區間；新價恰為 2.00 倍）；D23。榮譽制的技術理由是 **App B6**（不做硬 DRM） | ⏳ **2026-08-30 起整張卡標「還在計畫中」**（D3 → 同日再收緊）：卡片置灰，定價頁的行動點是 **disabled 的按鈕**、不再是連結 —— 在能力到位前不接受任何購買意向 |
| **持有 {sponsorSeats}（＝25）席以上的組織，可列名為支持組織** | 🔴 **BRD 沒有這一條**。創辦人 2026-08-30 新增（對標 Obsidian 的商業授權做法）<br>✅ **2026-08-31：那一頁已經存在** —— [`/enterprise`](./PageDescription/08_enterprise.md)，同日 `25` 也從三處字面值收斂進 `app/utils/pricing.ts` 的 `sponsorSeats`（紅線 1） | ⏳ 仍未實作（商業授權未開賣）。現出現於首頁商業卡、定價頁商業卡與 `/enterprise` 三處，三處都標「還在計畫中」 —— **這是它現在還不算未兌現承諾的唯一理由** |
| 🔴 **App runtime 從不檢查到期日、永不 time-bomb** | D14；[ADR-008「刻意不存在的欄位」](../appsync/ADR-008_授權憑證格式與離線驗證契約.md)（`exp`/`updatesUntil`/`maxVersion`/`machineId` 全數禁止） | ✅ **設計層面已保證** —— 欄位不存在，就沒有人能加那一行 `if` |
| 🔴 更新權到期後已安裝版本**永久正常運作，絕不鎖住或降級** | 同上；更新權執行點在官網下載閘門（D18） | ✅ 設計層面已保證 |
| ~~提供**企業發票**收據報帳~~ ⛔ **已從頁面移除** | 需要**統一編號** | ✅ **2026-08-30 解除曝險**：首頁與定價頁的商業卡不再承諾發票，改為「尚未開放購買，開放時一併公布發票與報帳方式」。[前置鏈 P2](../webplan/上線前置鏈.md) 公司登記仍未完成 —— **要重新寫回這句話，前置是 P2 完成，不是文案決定** |
| ⛔ **已移除**：商業授權「提供**專人諮詢**」 | **無依據**。[BRD-02 §3.1](../refer/BRD/BRD-02_商業模式與定價.md) 的前提是這條線「開發成本近乎為零」、功能完全同 Pro，賣的是合規與支持開發者 | ✅ 2026-08-30 移除。首頁版本本來就沒有這一句，兩頁不一致也一併收斂 |
| ⛔ **已移除**：「支援信用卡、**PayPal** 等」「授權碼**即時**寄送」 | **無依據**。金流平台尚未確定（BRD-02 內文提 Paddle、卷首註提 Polar），交付時效亦未經驗證 | ✅ 2026-08-30 改為「開放購買時一併公布」。定價頁 `faq.a6` 現在先講「尚未開放購買」，再講開放後的機制 |

---

## 🔴 目前有缺口的四條

| # | 缺口 | 對應 |
| --- | --- | --- |
| 1 | ~~**企業發票**已公開承諾，但沒有統編~~ → **2026-08-30 已從頁面撤下**（商業卡改「即將開放」）。缺口從「說出去但沒兌現」降級為「**還沒能力說**」 | [前置鏈 P2](../webplan/上線前置鏈.md)；重新寫回發票承諾前必須先完成 |
| 2 | ~~**電子報不追蹤**已寫在隱私頁，但 Buttondown 帳號尚未建立~~ → ✅ **2026-08-30 結案**：帳號已部署、開信追蹤已關 | D30 上線步驟已完成 |
| 3 | ~~**Free 版配額數字**的正本在 App repo，本 repo 只有副本~~ → ✅ **2026-08-30 結案**：創辦人確認 [refer/BRD/BRD-03](../refer/BRD/BRD-03_授權與Free-Pro邊界.md) **就是正本**，改配額直接以它為準 | [CLAUDE.md 紅線 3](../../CLAUDE.md) 已同步 |
| **5** | ~~早鳥名單收得到人，但**授權碼記錄在會員資料，而會員專區尚未建置**~~ → ✅ **2026-08-30 同日結案**：授權 email 由使用者購買時確認（預設帶入早鳥信箱），會員區做好再開賣 | [待討論問題 F18](../webplan/待討論問題.md)。⚠️ **留下一件實作待辦**：schema 要補 `user_id ↔ 授權 email` 的對應，否則會出現「憑證是 A 信箱、更新權掛在 B 帳號」的錯位（已回寫[接縫文件](../appsync/官網金流與授權發放接縫.md)） |
| 4 | **B5 護欄只釘住一半。** 2026-08-29 查核：`data-continuity.test.js` 只斷言 `appId`（`com.promptbox.app`）與 `productName`（`PromptBox`）；**userData 路徑、金鑰庫識別、DB 檔名、MCP server 識別都還沒有斷言** | 下載頁「資料原地保留」的依據強度低於它讀起來的樣子。補齊項目見 [身分字串凍結清單](../appsync/身分字串凍結清單.md)「護欄」一節與 [App 事實依據 §三](../appsync/App事實依據.md) |

第 2、3、5 條已於 2026-08-30 結案，第 1 條的曝險已由「把話撤下」解除（能力到位前不再寫回去）。

**現在只剩第 4 條**：它是**說出去的話只有一半有機器在守** —— 不會今天出事，會在改名那天出事。

> 🔴 **而改名已經是進行式**：2026-08-30 確認**目前沒有既有使用者**，
> [身分字串凍結清單](../appsync/身分字串凍結清單.md) 因此走 **§B 重選分支** ——
> 那是**最後一次**可以自由決定身分字串的機會。第 4 條的護欄要跟著新選的字串一起補，
> 不是補現在這組。
