# 更新日誌頁（`/changelog`、`/en/changelog`）

> 來源：`Src/app/pages/changelog.vue`、`Src/content/changelog/*.md`（13 檔）、`Src/content.config.ts`、`Src/i18n/locales/{zh-TW,en}/changelog.json`

---

## 1. 頁面類型定義 (Page Typology)

### 【事實提取】

- 承載 **13 筆同構條目**，每筆為一個版本發布實體，欄位結構一致：版號、日期、摘要、變更明細。
- 條目依日期字串**反向排序**，序位本身即攜帶語意——序位第一者自動取得「最新版本」標記，該標記不寫入資料本身。
- 條目被分為兩個層級：**主線版本**（11 筆，含完整變更明細）與**早期歷程**（2 筆，僅保留摘要，不展開明細）。
- 部分條目帶有版本語意標記：「重大版本」、「零連網里程碑」。
- 頁面末端含 **1 個資料收集表單**（email，分類 `updates`）。
- 非繁體中文語系下呈現內容語系落差聲明。
- 條目內容全部為**已發生事實的紀錄**，不含任何前瞻性承諾或路線圖。

### 【推論分類】

**聚合頁（Hub）／時序索引（Chronological Archive）**，兼具**發布公告節點**性質。

判定依據：頁面的組織原則是時間軸而非主題，條目之間無層級關係只有先後關係；且不存在下鑽節點——每筆版本紀錄在此即為完整形態，不指向獨立的版本詳情頁。其公告性質來自產品的零連網政策：由於 App 本身不做更新檢查，本頁與 email 名單構成使用者得知新版存在的僅有兩條管道。

---

## 2. 核心內容映射 (Content Ontology)

### 版本實體清單

| 條目 | 層級 | 承載的產品演進主題 |
| --- | --- | --- |
| v1.0 – v1.6 | 早期歷程 | 基礎建立：提示詞管理、資料夾、標籤、Markdown 預覽、變數樣板、內建教學內容 |
| v2.0 – v2.3.0 | 早期歷程 | 對外整合：MCP 初登場、Token 認證、系統匣常駐、便攜版與自訂安裝路徑 |
| v3.0.0 | 主線・重大版本 | 產品範疇的重新定義——由「收納工具」轉為「機密 Context 保險庫」；整庫靜態加密、AI 精準取用 |
| v3.1.0 | 主線 | 授權模型建立：卡片串聯、機密釋放（consent）、釋放稽核紀錄 |
| v3.2.0 – v3.2.1 | 主線 | 串聯槽、變數值繼承、強型別變數設定 |
| v3.2.2 | 主線 | 機密卡片的變數工作流恢復 |
| v3.2.3 | 主線 | 操作安全性優化：密碼可視切換、觸控目標放大、防呆 |
| v3.3.0 – v3.3.1 | 主線・零連網里程碑 | **架構立場的分水嶺**：移除更新器與所有外部連線、字型本地打包；更新通知改為 email |
| v3.4.0 – v3.4.2 | 主線 | 商業化邊界建立：四項免費配額（45／3／5／2），且明示配額只擋新增、既有資料不刪 |
| v3.5.0 | 主線 | MCP 可達性：速率上限、統一錯誤格式、9 款客戶端模板、開機常駐 |
| v3.6.0 | 主線 | **概念解耦**：密碼保護與 AI 可見性拆為兩條正交軸；授權快取與一鍵撤銷 |
| v3.7.0 | 主線 | 權責界定：AI 傳入的參數一律忽略，變數值僅來自人類策展；機密卡不對 AI 宣告可填參數（**2026-08-30 措辭修正**：原寫「連參數名都不對外揭露」，與 BRD-03 §9 規則 7 牴觸——參數名會出現在 MCP `tools/list`、且以明文存於 DB 快照欄位，不受密碼保護） |
| v3.7.1 | 主線・最新版本 | 試用版介面誠實化：功能標示 Beta、缺陷修復 |

### 其他資訊實體

- **版本語意標記體系**：最新版本（由序位推導）、重大版本、零連網里程碑
- **發布密度**：13 筆紀錄涵蓋 2026-02 至 2026-08 期間，日期為明確可比對的值
- **更新通知管道**：email 訂閱表單，附說明「App 不會、也永遠不會主動檢查更新，因此 email 是唯一通知管道」
- **當前版本聲明**：頁尾轉換召喚中複述 v3.7.1 的可用性

**本體特徵**：條目摘要並非變更項目的機械列舉，每筆皆帶一句主題陳述（如「Zero Outbound」「人類策展、AI 不代填」）。這使版本序列同時構成一條可讀的**產品立場演進敘事**——由功能累積（v1–v2）轉向安全架構（v3.0–v3.2），再轉向連網立場（v3.3）、商業邊界（v3.4）與權責界定（v3.6–v3.7）。

---

## 3. 連結拓樸分析 (Link Topology)

### 頁內錨點（In-page Fragment）

- [開場敘述「訂閱新版通知」] -> `#subscribe`（本頁末端表單）

### 內部路由（Internal Routing）

- [頁尾轉換召喚「前往下載最新版」] -> `/download`
- [導覽列主要按鈕] -> `/download`
- [主導覽與頁尾] -> 全站六個節點
- [語言切換] -> `/changelog` ⇄ `/en/changelog`

### 外部連結（External Outbound）

- [表單提交] -> `/api/subscribe` -> Buttondown 名單服務（伺服器端轉發）
- 除此之外**無任何使用者可觸發的站外導航**。

> **拓樸沿革事實**：本頁原先於開場處設有指向 `github.com/tsa206531/PromptBox/releases` 的外部連結，因該程式碼庫未公開而構成失效路由，現已改為指向本頁內的訂閱表單。此改動具有結構意義——原連結所回答的問題（「怎麼知道有新版」）與其替代目標（訂閱管道）在意圖上一致，因此拓樸重導後動線語意未被破壞。

---

## 4. 問題解決與意圖解析 (Problem Resolution & Intent)

### 【事實】

本頁提供：完整且帶日期的版本序列、每個主線版本的變更明細、版本重要性的分級標記，以及一條新版通知的登記管道。

### 【推論】

三種動機型態可由資訊配置反推：

1. **既有使用者的更新決策**——由於產品不做自動更新，使用者必須主動判斷「這一版改了什麼、值不值得手動下載安裝」。變更明細與摘要陳述服務於此判斷。
2. **評估者的維護活躍度查核**——潛在購買者在承諾付費前，需確認產品是否仍在持續開發。此動機不看單筆內容，而看**序列的密度與連續性**。13 筆紀錄集中於半年區間，本身即為此問題的答案。
3. **特定變更的追溯**——受某項行為變動影響的使用者（例如發現機密卡片的可見性行為與記憶不符），需定位該變更發生於哪一版。v3.6.0 與 v3.7.0 兩筆條目對行為變更的明確描述服務於此。

### 【價值判斷】

本頁在業務流程中解決兩個問題。

**其一，以可稽核的紀錄取代口說的活躍度證明。** 「這個產品還有人在維護嗎」是評估任何獨立軟體時的實質風險，而該問題無法透過宣傳語句回答——任何產品都會宣稱自己在持續開發。本頁的價值不在於任一筆條目的內容，而在於序列本身的存在：帶日期、可比對、且無法事後偽造連續性的紀錄，構成一項可被外部驗證的維護證據。這消除的是「產品可能被棄置」的資訊落差，其效果尤其作用於買斷制商品——因為買斷者承擔的正是「付費之後產品停止演進」的風險。

**其二，承接零連網政策所外部化的通知責任。** 產品移除所有對外連線後，「有新版了」這個訊息在技術上無法由產品自行送達使用者。本頁因此不只是歷史紀錄，而是該通知機制的**被動端**——使用者主動來查；而頁內表單則是其**主動端**——使用者留下地址由系統推送。兩者共同構成對一條被刻意切斷的通道的完整重建。

此外，版本序列所呈現的立場演進（尤其 v3.3 的零連網、v3.6 的概念解耦、v3.7 的權責界定），使本頁在說服層面產生了非預期的作用：它以行為紀錄佐證了首頁的隱私主張並非行銷措辭，而是有時間軸可循的架構決策。
